---
phase: 19-curriculum-manifest-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vocabulary/curricula/vocab-manifest-tysk1-vg1.json
  - api/vocab/README.md
autonomous: true
requirements:
  - INTEG-01
  - INTEG-02

must_haves:
  truths:
    - "vocab-manifest-tysk1-vg1.json contains zero instances of grammar_present in German lesson feature arrays"
    - "api/vocab/README.md German grammar features example uses grammar_presens"
    - "All other feature IDs in the manifest remain unchanged (grammar_pronouns_*, grammar_articles, grammar_plural, grammar_accusative_*, grammar_comparative, grammar_superlative, grammar_dative)"
  artifacts:
    - path: "vocabulary/curricula/vocab-manifest-tysk1-vg1.json"
      provides: "Curriculum manifest with corrected grammar_presens IDs"
      contains: "grammar_presens"
    - path: "api/vocab/README.md"
      provides: "API documentation with corrected grammar_presens example"
      contains: "grammar_presens"
  key_links:
    - from: "vocabulary/curricula/vocab-manifest-tysk1-vg1.json"
      to: "vocabulary/grammar-features.json"
      via: "feature ID string match"
      pattern: "grammar_presens"
    - from: "api/vocab/README.md"
      to: "vocabulary/grammar-features.json"
      via: "documentation example matching actual feature IDs"
      pattern: "grammar_presens"
---

<objective>
Replace all stale `grammar_present` feature IDs with `grammar_presens` in the German curriculum manifest and API README documentation.

Purpose: Phase 17 renamed the German presens grammar feature from `grammar_present` to `grammar_presens` in grammar-features.json and the v2 lookup handler. Two downstream files still reference the old ID: the curriculum manifest (31 occurrences) and the API README (1 occurrence). This causes a mismatch between what the API emits and what curriculum data references.

Output: Both files updated with consistent `grammar_presens` IDs matching grammar-features.json.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-api-fixes/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace grammar_present with grammar_presens in curriculum manifest</name>
  <files>vocabulary/curricula/vocab-manifest-tysk1-vg1.json</files>
  <action>
In `vocabulary/curricula/vocab-manifest-tysk1-vg1.json`, replace every occurrence of the string `"grammar_present"` with `"grammar_presens"`.

There are 31 occurrences across lesson feature arrays (lessons 1.1 through 12.3). This is a global string replacement — no occurrences should remain.

Do NOT touch any other feature IDs (grammar_pronouns_ich_du, grammar_pronouns_singular_wir, grammar_pronouns_all, grammar_articles, grammar_plural, grammar_accusative_indefinite, grammar_accusative_definite, grammar_comparative, grammar_superlative, grammar_dative). Only `grammar_present` changes.

After replacement, validate the JSON is still valid by parsing it with `node -e "JSON.parse(require('fs').readFileSync('vocabulary/curricula/vocab-manifest-tysk1-vg1.json','utf8')); console.log('Valid JSON')"`.
  </action>
  <verify>
Run: `grep -c "grammar_present" vocabulary/curricula/vocab-manifest-tysk1-vg1.json` — must return 0.
Run: `grep -c "grammar_presens" vocabulary/curricula/vocab-manifest-tysk1-vg1.json` — must return 31.
Run: `node -e "JSON.parse(require('fs').readFileSync('vocabulary/curricula/vocab-manifest-tysk1-vg1.json','utf8')); console.log('Valid JSON')"` — must print "Valid JSON".
  </verify>
  <done>vocab-manifest-tysk1-vg1.json has zero instances of grammar_present and 31 instances of grammar_presens. JSON is valid.</done>
</task>

<task type="auto">
  <name>Task 2: Replace grammar_present with grammar_presens in API README</name>
  <files>api/vocab/README.md</files>
  <action>
In `api/vocab/README.md`, find the German grammar features example (around line 170) where `"id": "grammar_present"` appears and change it to `"id": "grammar_presens"`.

This is a single occurrence in the documentation example JSON block. The surrounding context (name, nameEn, description, category, appliesTo) should remain unchanged.
  </action>
  <verify>
Run: `grep -c "grammar_present" api/vocab/README.md` — must return 0.
Run: `grep "grammar_presens" api/vocab/README.md` — must show the corrected line.
  </verify>
  <done>api/vocab/README.md German grammar features example uses grammar_presens (not grammar_present). No other occurrences of grammar_present remain.</done>
</task>

</tasks>

<verification>
1. `grep -r "grammar_present[^s]" vocabulary/curricula/ api/vocab/README.md` — must return nothing (no stale IDs anywhere in target files)
2. `grep -c "grammar_presens" vocabulary/curricula/vocab-manifest-tysk1-vg1.json` — must return 31
3. `grep "grammar_presens" api/vocab/README.md` — must show corrected documentation example
4. `node -e "JSON.parse(require('fs').readFileSync('vocabulary/curricula/vocab-manifest-tysk1-vg1.json','utf8')); console.log('Valid JSON')"` — JSON still valid
5. Run `npm run validate:all` — all validation passes (manifest and README are not covered by schema validation, but ensures no regression)
</verification>

<success_criteria>
- Zero instances of `grammar_present` in vocab-manifest-tysk1-vg1.json
- Zero instances of `grammar_present` in api/vocab/README.md
- 31 instances of `grammar_presens` in vocab-manifest-tysk1-vg1.json
- 1 instance of `grammar_presens` in api/vocab/README.md
- Valid JSON in manifest file
- All existing validation scripts still pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-curriculum-manifest-fix/19-01-SUMMARY.md`
</output>
