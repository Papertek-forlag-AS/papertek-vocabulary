---
phase: 21-translation-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/merge-translations.js
  - vocabulary/translations/de-nb/generalbank.json
  - vocabulary/translations/de-nb/nounbank.json
  - vocabulary/translations/de-nb/verbbank.json
  - vocabulary/translations/de-nb/manifest.json
  - vocabulary/translations/de-en/generalbank.json
  - vocabulary/translations/de-en/nounbank.json
  - vocabulary/translations/de-en/verbbank.json
  - vocabulary/translations/de-en/manifest.json
  - scripts/build-search-index.js
  - api/vocab/v2/lookup/[language]/[wordId].js
  - package.json
autonomous: true
requirements: [TRANS-01, TRANS-02]

must_haves:
  truths:
    - "de-nb/ contains translations for ALL entries that had a translation in either de-nb/ or de-nb-dict/"
    - "de-en/ contains translations for ALL entries that had a translation in either de-en/ or de-en-dict/"
    - "build-search-index.js reads only from de-nb/ and de-en/ (no -dict/ references)"
    - "v2 lookup handler reads from single translation directory (no -dict/ fallback)"
  artifacts:
    - path: "scripts/merge-translations.js"
      provides: "ESM script merging core + dict translations into single dirs"
    - path: "vocabulary/translations/de-nb/nounbank.json"
      provides: "Merged Norwegian noun translations (331 core + 1310 dict = 1641)"
    - path: "vocabulary/translations/de-nb/verbbank.json"
      provides: "Merged Norwegian verb translations (148 core + 531 dict = 679)"
    - path: "vocabulary/translations/de-nb/generalbank.json"
      provides: "Merged Norwegian general translations (186 core + 487 dict = 673)"
    - path: "vocabulary/translations/de-en/nounbank.json"
      provides: "Merged English noun translations (331 core + 1310 dict = 1641)"
    - path: "vocabulary/translations/de-en/verbbank.json"
      provides: "Merged English verb translations (148 core + 531 dict = 679)"
    - path: "vocabulary/translations/de-en/generalbank.json"
      provides: "Merged English general translations (187 core + 487 dict = 674)"
  key_links:
    - from: "scripts/build-search-index.js"
      to: "vocabulary/translations/de-nb/"
      via: "single translation dir read (no -dict/ fallback)"
      pattern: "readFileSync.*de-nb/"
    - from: "api/vocab/v2/lookup/[language]/[wordId].js"
      to: "vocabulary/translations/{pair}/"
      via: "single translation dir read (no -dict/ fallback)"
      pattern: "translationBankPath.*translations.*translationPair"
---

<objective>
Merge German translation directories (de-nb + de-nb-dict, de-en + de-en-dict) into single directories per language pair, then update all codebase references that read from the -dict/ directories.

Purpose: Eliminate the dual translation directory pattern, completing the data consolidation alongside Phase 20's bank merge.
Output: Merged translation files in de-nb/ and de-en/, updated scripts and API handlers reading from single directories.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-bank-manifest-consolidation/20-01-SUMMARY.md
@.planning/phases/20-bank-manifest-consolidation/20-02-SUMMARY.md
@scripts/merge-banks.js
@scripts/build-search-index.js
@api/vocab/v2/lookup/[language]/[wordId].js
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create translation merge script and merge all translations</name>
  <files>
    scripts/merge-translations.js
    vocabulary/translations/de-nb/generalbank.json
    vocabulary/translations/de-nb/nounbank.json
    vocabulary/translations/de-nb/verbbank.json
    vocabulary/translations/de-nb/manifest.json
    vocabulary/translations/de-en/generalbank.json
    vocabulary/translations/de-en/nounbank.json
    vocabulary/translations/de-en/verbbank.json
    vocabulary/translations/de-en/manifest.json
    package.json
  </files>
  <action>
    Create `scripts/merge-translations.js` (ESM) following the same pattern as `scripts/merge-banks.js`.

    The script merges translation files from -dict/ directories INTO the existing core translation directories:

    **Data discovery (confirmed):**
    - Only 3 bank types have -dict/ translations: nounbank, verbbank, generalbank
    - Zero overlaps between core and dict translation keys (disjoint sets)
    - 5 banks (adjectivebank, articlesbank, numbersbank, phrasesbank, pronounsbank) only exist in core dirs — no action needed for these

    **Merge logic for each language pair (de-nb, de-en):**
    1. For each of the 3 banks with dict translations (nounbank, verbbank, generalbank):
       a. Read the core translation file (e.g., `vocabulary/translations/de-nb/nounbank.json`)
       b. Read the dict translation file (e.g., `vocabulary/translations/de-nb-dict/nounbank.json`)
       c. Merge: start with core data, add all dict entries. Since there are 0 overlaps, this is a simple union. If an overlap were found (defensive), log it and prefer the core version.
       d. Sort entries by key (ASCII sort — project convention)
       e. Preserve `_metadata` from core file if present, update totalWords count
       f. Write merged file back to the core translation directory
    2. For the 5 core-only banks: leave them untouched (already correct)

    **Manifest handling:**
    Regenerate `manifest.json` in both de-nb/ and de-en/ with updated word counts reflecting the merged data. Read the existing manifest, update the `totalWords` and per-file counts by counting actual entries in each file.

    **Gap reporting:**
    After merging, compare merged translation entry IDs against the merged bank entry IDs from `vocabulary/banks/de/`. Log a summary of entries in banks that are missing translations (informational only, not blocking). Format: `{bank}: {missing_count} entries missing translations out of {total}`.

    **Register script:**
    Add `"merge:translations": "node scripts/merge-translations.js"` to package.json scripts.

    **Expected counts after merge:**
    - de-nb/nounbank.json: 1641 entries (331 core + 1310 dict)
    - de-nb/verbbank.json: 679 entries (148 core + 531 dict)
    - de-nb/generalbank.json: 673 entries (186 core + 487 dict)
    - de-en/nounbank.json: 1641 entries (331 core + 1310 dict)
    - de-en/verbbank.json: 679 entries (148 core + 531 dict)
    - de-en/generalbank.json: 674 entries (187 core + 487 dict)
  </action>
  <verify>
    Run `node scripts/merge-translations.js` and verify:
    1. Script exits 0
    2. de-nb/nounbank.json has 1641 entries (excl. _metadata)
    3. de-nb/verbbank.json has 679 entries
    4. de-nb/generalbank.json has 673 entries
    5. de-en/nounbank.json has 1641 entries
    6. de-en/verbbank.json has 679 entries
    7. de-en/generalbank.json has 674 entries
    8. All entries from de-nb-dict/ are present in merged de-nb/ files
    9. All entries from de-en-dict/ are present in merged de-en/ files
    10. manifest.json files updated with correct totalWords counts
  </verify>
  <done>
    Merged translation files contain the union of core + dict entries for all 3 affected bank types in both language pairs. Manifest files reflect accurate counts.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update all codebase references from -dict/ to single translation directories</name>
  <files>
    scripts/build-search-index.js
    api/vocab/v2/lookup/[language]/[wordId].js
    package.json
  </files>
  <action>
    Update all active codebase files that reference the -dict/ translation directories or the old vocabulary/dictionary/de/ path.

    **1. scripts/build-search-index.js:**
    - Remove the `dictNb` and `dictEn` objects (lines 64-73) that load from de-nb-dict/ and de-en-dict/
    - Update the `currNb` and `currEn` objects to be simply `nb` and `en` (rename for clarity since there's no longer a "curr" vs "dict" distinction)
    - Simplify `getTranslation()` function: remove the dictMap parameter and fallback logic. Now it only reads from a single map per language.
    - Update `buildEntry()` calls to pass simplified translation maps
    - Update file header comment: remove references to "dict-only" translation directories and the Phase 21 note
    - Ensure output is unchanged: rebuild search index and verify same 3454 entries, 144 verbs with pp

    **2. api/vocab/v2/lookup/[language]/[wordId].js:**
    - Remove `dictTranslationBankPath` construction (line 163: `const dictTranslationBankPath = ...`)
    - Remove the dict fallback block (lines 172-175: the `if (!translationEntry && fs.existsSync(dictTranslationBankPath))` block)
    - The remaining logic: read from `translationBankPath` only (which points to the now-merged translation dir)

    **3. package.json:**
    - Remove `validate:nouns:dict` script (references vocabulary/dictionary/de/ which will be deleted)
    - Remove `validate:verbs:dict` script (same reason)
    - Update `validate:all` to remove the `npm run validate:nouns:dict && npm run validate:verbs:dict &&` portion
    - These validation scripts are no longer meaningful since there are no separate dict bank files — validators should run against the merged banks under vocabulary/banks/de/ (that's a Phase 23 concern; for now just remove the broken references)

    **Note:** Scripts under `.planning/milestones/` (old phase scripts like generate-stubs.js, generate-comparison.js etc.) are historical archives — do NOT update them. Only update active scripts in `scripts/` and `api/`.

    **Note:** `scripts/verify-integration.js`, `scripts/merge-banks.js`, `scripts/sync-perfektum.js`, `scripts/sync-preteritum.js`, `scripts/sync-nouns.js`, `scripts/fix-noun-fields.js`, `scripts/fix-verb-fields.js`, `scripts/fix-manifest-counts.js` all reference `vocabulary/dictionary/de/`. These are one-shot migration/fix scripts from prior phases. They are not run in production and will naturally break once the directory is deleted in Task 2 of Plan 02. Do NOT update them — they served their purpose. If needed in the future, users can check git history.
  </action>
  <verify>
    1. `grep -r "de-nb-dict\|de-en-dict" scripts/build-search-index.js api/` returns NO matches
    2. `grep -r "dictionary/de" package.json` returns NO matches
    3. Run `npm run build:search-index` — outputs 3454 entries, 144 verbs with pp (unchanged)
    4. `grep "dictTranslationBankPath" api/vocab/v2/lookup/\[language\]/\[wordId\].js` returns NO matches
  </verify>
  <done>
    All active scripts and API handlers read from single translation directories. No active code references -dict/ translation paths. Search index rebuilds identically.
  </done>
</task>

</tasks>

<verification>
1. Translation merge integrity: merged files contain exact union of core + dict entries (count check per bank per language pair)
2. No broken references: `grep -r "de-nb-dict\|de-en-dict" scripts/ api/ package.json` returns no matches (excluding comments/docs only)
3. Search index unchanged: `npm run build:search-index` produces 3454 entries with identical structure
4. v2 lookup handler: no -dict/ fallback code remains
5. Gap report: any entries missing translations are logged (informational)
</verification>

<success_criteria>
- 6 translation files merged (3 banks x 2 language pairs) with correct entry counts
- build-search-index.js reads from single dirs and produces identical output
- v2 lookup handler simplified to single translation dir read
- package.json cleaned of old vocabulary/dictionary/de references
- Translation manifests updated with accurate counts
</success_criteria>

<output>
After completion, create `.planning/phases/21-translation-consolidation/21-01-SUMMARY.md`
</output>
