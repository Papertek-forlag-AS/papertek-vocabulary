---
phase: 17-api-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/vocab/v2/lookup/[language]/[wordId].js
  - vocabulary/grammar-features.json
autonomous: true
requirements: [API-01, API-02, API-03]

must_haves:
  truths:
    - "A v2 lookup for a German verb returns grammar feature ID 'grammar_presens' (not 'grammar_present')"
    - "A v2 lookup for an adjective with genitiv declension data returns 'grammar_adjective_genitive' in grammarFeatures"
    - "A v2 lookup for teuer_adj returns a 'declensionAlternatives' field containing the alternative declension forms"
  artifacts:
    - path: "api/vocab/v2/lookup/[language]/[wordId].js"
      provides: "v2 lookup handler with corrected grammar feature IDs and new field surfacing"
      contains: "grammar_presens"
    - path: "vocabulary/grammar-features.json"
      provides: "Grammar features metadata with consistent German presens ID"
      contains: "grammar_presens"
  key_links:
    - from: "api/vocab/v2/lookup/[language]/[wordId].js"
      to: "vocabulary/grammar-features.json"
      via: "grammar feature ID consistency"
      pattern: "grammar_presens"
    - from: "api/vocab/v2/lookup/[language]/[wordId].js"
      to: "vocabulary/dictionary/de/adjectivebank.json"
      via: "declension genitiv detection and declension_alternatives surfacing"
      pattern: "declension_alternatives|declension\\.positiv\\.stark\\.genitiv"
---

<objective>
Fix three issues in the v2 lookup API handler: rename the German presens grammar feature ID for consistency, emit the adjective genitive feature flag, and surface the declension_alternatives field.

Purpose: Ensure the v2 API response is complete and consistently named so downstream clients (Leksihjelp Chrome extension) can rely on feature flags and data fields without special-casing.

Output: Updated v2 lookup handler and grammar-features.json with all three fixes applied.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@api/vocab/v2/lookup/[language]/[wordId].js
@vocabulary/grammar-features.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix grammar_present ID to grammar_presens for German and add adjective genitive feature flag</name>
  <files>
    api/vocab/v2/lookup/[language]/[wordId].js
    vocabulary/grammar-features.json
  </files>
  <action>
**API-01 — Rename presens feature ID (German only):**

1. In `api/vocab/v2/lookup/[language]/[wordId].js`, line 237:
   - Change `grammarFeatures.push('grammar_present')` to `grammarFeatures.push('grammar_presens')`
   - This is the ONLY place in the handler that pushes `grammar_present` for German verb presens detection.
   - Do NOT change `grammar_preteritum` or `grammar_perfektum` — those already use German names.

2. In `vocabulary/grammar-features.json`, in the `de.features` array (first entry, around line 12-20):
   - Change `"id": "grammar_present"` to `"id": "grammar_presens"`
   - Do NOT change the Spanish (`es`) or French (`fr`) entries — they correctly use `grammar_present` for their own language conventions (`presente`, `présent`).

**API-02 — Add grammar_adjective_genitive feature flag:**

3. In the v2 lookup handler, in the grammar features section (around line 260-265), AFTER the `grammar_adjective_declension` push block, add a new check:
   ```javascript
   // Adjective genitive declension — check if genitiv key exists in any declension type
   if (entry.declension?.positiv?.stark?.genitiv ||
       entry.declension?.positiv?.schwach?.genitiv ||
       entry.declension?.positiv?.gemischt?.genitiv) {
     grammarFeatures.push('grammar_adjective_genitive');
   }
   ```
   This detects adjective entries that have genitiv data in their declension tables. The feature `grammar_adjective_genitive` already exists in grammar-features.json (added in Phase 11), so no change needed there.
  </action>
  <verify>
1. Grep the handler for `grammar_present` — should find ZERO occurrences (only `grammar_presens`).
2. Grep grammar-features.json for German section — should show `"id": "grammar_presens"`.
3. Grep grammar-features.json for Spanish/French sections — should still show `"id": "grammar_present"`.
4. Grep the handler for `grammar_adjective_genitive` — should find the new push statement.
  </verify>
  <done>
The v2 handler pushes 'grammar_presens' (not 'grammar_present') for German verb presens, the German grammar-features.json entry ID matches, and adjective entries with genitiv declension data get the 'grammar_adjective_genitive' feature flag.
  </done>
</task>

<task type="auto">
  <name>Task 2: Surface declension_alternatives field in v2 lookup response</name>
  <files>
    api/vocab/v2/lookup/[language]/[wordId].js
  </files>
  <action>
**API-03 — Surface declension_alternatives:**

1. In the v2 lookup handler, in the "Adjective-specific fields" section (around line 229), AFTER the `if (entry.comparison)` line, add:
   ```javascript
   if (entry.declension_alternatives) response.declensionAlternatives = entry.declension_alternatives;
   ```
   This follows the existing pattern of camelCasing snake_case data fields (e.g., `declension_type` -> `declensionType` on line 227, `weak_masculine` -> `weakMasculine` on line 225).

2. Currently only `teuer_adj` has `declension_alternatives` in the dictionary adjectivebank. The field contains a full parallel declension table (positiv with stark/schwach/gemischt, each with nominativ/akkusativ/dativ/genitiv) using the alternative spelling forms (e.g., "teuerer" instead of "teurer").

3. Place the new line in the adjective section, near other adjective fields, to keep the response builder organized by word type.
  </action>
  <verify>
1. Grep the handler for `declensionAlternatives` — should find exactly one occurrence.
2. Grep the handler for `declension_alternatives` — should find it in the conditional check.
3. Manual verification: trace through the handler logic with a mental model of `teuer_adj` entry — confirm the field would be included in the response.
  </verify>
  <done>
A v2 lookup for `teuer_adj` (or any future adjective with `declension_alternatives`) includes the `declensionAlternatives` field in the response JSON, following the project's camelCase convention.
  </done>
</task>

</tasks>

<verification>
All three API requirements verified:

1. **API-01**: `grep -c 'grammar_present' api/vocab/v2/lookup/\[language\]/\[wordId\].js` returns 0 (no legacy ID); `grep -c 'grammar_presens' ...` returns 1.
2. **API-02**: `grep 'grammar_adjective_genitive' api/vocab/v2/lookup/\[language\]/\[wordId\].js` finds the push statement.
3. **API-03**: `grep 'declensionAlternatives' api/vocab/v2/lookup/\[language\]/\[wordId\].js` finds the response assignment.

Cross-check: grammar-features.json German entry uses `grammar_presens`; Spanish and French still use `grammar_present`.
</verification>

<success_criteria>
- The string 'grammar_present' does not appear in the v2 lookup handler (replaced by 'grammar_presens')
- The German entry in grammar-features.json has id 'grammar_presens'
- Spanish and French entries in grammar-features.json are unchanged
- Adjective entries with genitiv declension data get 'grammar_adjective_genitive' in grammarFeatures
- Entries with declension_alternatives get a camelCased 'declensionAlternatives' field in the response
</success_criteria>

<output>
After completion, create `.planning/phases/17-api-fixes/17-01-SUMMARY.md`
</output>
