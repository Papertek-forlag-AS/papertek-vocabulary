---
phase: 03-bank-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vocabulary/schema/adjective.schema.json
  - vocabulary/core/de/adjectivebank.json
  - vocabulary/dictionary/de/adjectivebank.json
  - vocabulary/translations/de-nb/adjectivebank.json
  - vocabulary/translations/de-en/adjectivebank.json
  - vocabulary/core/de/generalbank.json
  - vocabulary/dictionary/de/generalbank.json
  - vocabulary/translations/de-nb/generalbank.json
  - vocabulary/translations/de-en/generalbank.json
  - vocabulary/dictionary/de/search-index.json
  - vocabulary/dictionary/de/search-index.pretty.json
  - vocabulary/dictionary/de/manifest.json
  - vocabulary/curricula/vocab-manifest-tysk1-vg1.json
  - vocabulary/curricula/vocab-manifest-us-9.json
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "Schema validation passes on all remaining adjective entries (106 after removals) with translations optional"
    - "beste_adj does not exist in any vocabulary file (core, dictionary, translations, search index, curricula)"
    - "Lieblings- exists in generalbank (core + dictionary) with type expr and _id lieblings-_expr"
    - "Lieblings- does not exist in any adjective bank file"
    - "Manifest counts reflect 106 adjectives and 186 general entries"
    - "Search index references lieblings-_expr in generalbank, not lieblings-_adj in adjectivebank"
  artifacts:
    - path: "vocabulary/schema/adjective.schema.json"
      provides: "Adjective schema with translations optional"
      contains: "\"required\": []"
    - path: "vocabulary/core/de/generalbank.json"
      provides: "Lieblings- entry in generalbank"
      contains: "lieblings-_expr"
    - path: "vocabulary/dictionary/de/manifest.json"
      provides: "Updated bank entry counts"
      contains: "106"
  key_links:
    - from: "vocabulary/core/de/generalbank.json"
      to: "vocabulary/translations/de-nb/generalbank.json"
      via: "lieblings-_expr key in both files"
      pattern: "lieblings-_expr"
    - from: "vocabulary/dictionary/de/generalbank.json"
      to: "vocabulary/dictionary/de/search-index.pretty.json"
      via: "search index entry with id lieblings-_expr"
      pattern: "lieblings-_expr"
---

<objective>
Clean up the German adjective bank by fixing schema validation (making translations optional), removing the misclassified beste_adj entry, and reclassifying the Lieblings- prefix from the adjective bank to the general bank. Update all downstream files (translations, search index, curricula manifests, bank manifest) to stay in sync.

Purpose: Achieve zero schema validation errors on the adjective bank and eliminate corrupt/misclassified entries before any new adjective data is added in later phases.
Output: A clean adjective bank with 106 valid entries, Lieblings- in generalbank, beste_adj fully removed, and all file counts/references consistent.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-bank-cleanup/03-CONTEXT.md
@.planning/phases/03-bank-cleanup/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix schema and remove beste_adj from all files</name>
  <files>
    vocabulary/schema/adjective.schema.json
    vocabulary/core/de/adjectivebank.json
    vocabulary/dictionary/de/adjectivebank.json
    vocabulary/translations/de-nb/adjectivebank.json
    vocabulary/translations/de-en/adjectivebank.json
    vocabulary/dictionary/de/search-index.json
    vocabulary/dictionary/de/search-index.pretty.json
    vocabulary/curricula/vocab-manifest-tysk1-vg1.json
    vocabulary/curricula/vocab-manifest-us-9.json
  </files>
  <action>
    **Step 1 — CLEAN-03: Make translations optional in adjective schema.**

    In `vocabulary/schema/adjective.schema.json`, change the `required` array in the `adjectiveEntry` definition (currently line 46):
    - Change `"required": ["translations"]` to `"required": []`
    - This makes translations optional, matching the project's design where translations live in separate translation files.

    **Step 2 — CLEAN-01: Remove beste_adj from all files.**

    beste_adj is a superlative form of "gut" — it does not belong as a standalone adjective entry. Remove its keyed object from ALL of these files (remove the entire `"beste_adj": { ... }` block including all nested content):

    1. `vocabulary/core/de/adjectivebank.json` — Remove the `"beste_adj"` entry (around line 527-532). Be careful to handle trailing/leading commas so JSON stays valid.
    2. `vocabulary/dictionary/de/adjectivebank.json` — Remove the `"beste_adj"` entry (around line 840-848).
    3. `vocabulary/translations/de-nb/adjectivebank.json` — Remove the `"beste_adj"` entry (around line 797-799).
    4. `vocabulary/translations/de-en/adjectivebank.json` — Remove the `"beste_adj"` entry (around line 321-323).
    5. `vocabulary/dictionary/de/search-index.pretty.json` — Remove the entire object `{ "id": "beste_adj", ... }` from the `entries` array (around line 4692-4699). Remove the trailing/leading comma to keep valid JSON.
    6. `vocabulary/dictionary/de/search-index.json` — This is a minified single-line file. Remove the `{"id":"beste_adj",...}` object from the entries array. Use a JSON parse-edit-serialize approach: read the file, parse JSON, filter out the entry with id "beste_adj" from the entries array, serialize back as compact JSON (no whitespace).
    7. `vocabulary/curricula/vocab-manifest-tysk1-vg1.json` — Remove `"beste_adj"` from the vocabulary ID array (line 666). Remove trailing comma if needed.
    8. `vocabulary/curricula/vocab-manifest-us-9.json` — Remove `"beste_adj"` from the vocabulary ID array (line 8). Remove trailing comma if needed.

    **Commit this task** with message: `fix(03-bank-cleanup): make translations optional in schema and remove beste_adj from all banks`
    Include in commit body: "CLEAN-03: Changed adjective schema required field from [\"translations\"] to [] — translations stored in separate files by design. CLEAN-01: Removed beste_adj (superlative of gut) from core bank, dictionary bank, 2 translation files, 2 search index files, and 2 curriculum manifests. Entry count: 107 adjectives remain."
  </action>
  <verify>
    1. `grep -r "beste_adj" vocabulary/` returns zero matches
    2. All modified JSON files are valid: `node -e "for (const f of ['vocabulary/core/de/adjectivebank.json','vocabulary/dictionary/de/adjectivebank.json','vocabulary/translations/de-nb/adjectivebank.json','vocabulary/translations/de-en/adjectivebank.json','vocabulary/dictionary/de/search-index.pretty.json','vocabulary/dictionary/de/search-index.json','vocabulary/curricula/vocab-manifest-tysk1-vg1.json','vocabulary/curricula/vocab-manifest-us-9.json']) { JSON.parse(require('fs').readFileSync(f,'utf8')); console.log('OK:', f); }"`
    3. `vocabulary/schema/adjective.schema.json` contains `"required": []`
  </verify>
  <done>
    beste_adj appears in zero vocabulary files. Schema has translations as optional. All JSON files parse without error. 107 adjective entries remain in the core bank.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reclassify Lieblings- from adjective bank to general bank</name>
  <files>
    vocabulary/core/de/adjectivebank.json
    vocabulary/dictionary/de/adjectivebank.json
    vocabulary/translations/de-nb/adjectivebank.json
    vocabulary/translations/de-en/adjectivebank.json
    vocabulary/core/de/generalbank.json
    vocabulary/dictionary/de/generalbank.json
    vocabulary/translations/de-nb/generalbank.json
    vocabulary/translations/de-en/generalbank.json
    vocabulary/dictionary/de/search-index.json
    vocabulary/dictionary/de/search-index.pretty.json
    vocabulary/dictionary/de/manifest.json
  </files>
  <action>
    **Step 1 — Remove Lieblings- from adjective files.**

    Remove the `"lieblings-_adj"` keyed entry from:
    1. `vocabulary/core/de/adjectivebank.json` — Remove `"lieblings-_adj": { "word": "Lieblings-", "_id": "lieblings-_adj" }` block (around line 518-521). Fix commas.
    2. `vocabulary/dictionary/de/adjectivebank.json` — Remove `"lieblings-_adj": { ... }` block (around line 825-831). Fix commas.
    3. `vocabulary/translations/de-nb/adjectivebank.json` — Remove `"lieblings-_adj": { ... }` block (around line 780-785, includes translation + explanation). Fix commas.
    4. `vocabulary/translations/de-en/adjectivebank.json` — Remove `"lieblings-_adj": { ... }` block (around line 315-317). Fix commas.

    **Step 2 — Add Lieblings- to general bank files with new ID `lieblings-_expr`.**

    Use type `expr` (expression — the most neutral type for a word-forming prefix that doesn't fit standard grammatical categories).

    1. `vocabulary/core/de/generalbank.json` — Add a new entry at the end of the object (before the closing `}`):
       ```json
       "lieblings-_expr": {
         "word": "Lieblings-",
         "type": "expr",
         "_id": "lieblings-_expr"
       }
       ```

    2. `vocabulary/dictionary/de/generalbank.json` — Add a new entry at the end of the object (before the closing `}`):
       ```json
       "lieblings-_expr": {
         "word": "Lieblings-",
         "type": "expr",
         "_id": "lieblings-_expr",
         "curriculum": true,
         "cefr": "A1",
         "frequency": 25599
       }
       ```
       (Preserve the curriculum, cefr, and frequency values from the old dictionary adjective entry.)

    3. `vocabulary/translations/de-nb/generalbank.json` — Add at the end:
       ```json
       "lieblings-_expr": {
         "translation": "favoritt-",
         "explanation": {
           "_description": "Prefiks som betyr favoritt. F.eks. Lieblingsfarbe (favorittfarge)."
         }
       }
       ```
       (Preserve the exact translation and explanation from the old adjective translation file.)

    4. `vocabulary/translations/de-en/generalbank.json` — Add at the end:
       ```json
       "lieblings-_expr": {
         "translation": "favorite"
       }
       ```

    **Step 3 — Update search index.**

    In `vocabulary/dictionary/de/search-index.pretty.json`:
    - Remove the object with `"id": "lieblings-_adj"` from the `entries` array (around line 20290-20298).
    - Add a new object to the `entries` array (at the end, before the closing `]`):
      ```json
      {
        "id": "lieblings-_expr",
        "w": "Lieblings-",
        "t": "expr",
        "f": 25599,
        "b": "generalbank",
        "tr": {
          "nb": "favoritt-",
          "en": "favorite"
        }
      }
      ```
      (Copy the structure from the old entry but change id to `lieblings-_expr`, type to `expr`, bank to `generalbank`.)

    In `vocabulary/dictionary/de/search-index.json` (minified):
    - Parse JSON, filter out entry with `id === "lieblings-_adj"`, add the new `lieblings-_expr` entry, serialize as compact JSON.

    **Step 4 — Update search index `_meta.totalEntries`.**

    The total entry count in the search index does not change (one removed, one added). Verify `_meta.totalEntries` is still 3196 in both search index files.

    **Step 5 — Update manifest counts.**

    In `vocabulary/dictionary/de/manifest.json`:
    - Change `"adjectivebank.json": 108` to `"adjectivebank.json": 106` (removed beste_adj in Task 1 and lieblings-_adj here)
    - Change `"generalbank.json": 185` to `"generalbank.json": 186` (added lieblings-_expr)
    - Update `"totalWords"` in `_metadata`: 868 - 2 + 1 = 867 (net: removed 2 adjectives, added 1 general)
    - Update `"curriculumWords"`: same calculation, 868 - 2 + 1 = 867
    - Update top-level `"totalWords"`: 3196 - 2 + 1 = 3195
    - Update top-level `"curriculumWords"`: 868 - 2 + 1 = 867
    - Wait — check whether beste_adj had `"curriculum": true` in the dictionary bank. If yes, curriculumWords goes down. Let me verify: beste_adj in dictionary/de/adjectivebank.json has `"curriculum": true`. lieblings-_adj also has `"curriculum": true`. So net curriculum change: -2 +1 = -1 → 867.
    - `"dictionaryOnlyWords"` stays 0 in _metadata (curriculum subset) but top-level `"dictionaryOnlyWords"`: 3195 - 867 = 2328. Same as before? 3196-868=2328 → 3195-867=2328. Yes, unchanged.

    **Commit this task** with message: `fix(03-bank-cleanup): reclassify Lieblings- to generalbank and update manifest`
    Include in commit body: "CLEAN-02: Moved Lieblings- from adjectivebank (lieblings-_adj) to generalbank (lieblings-_expr, type: expr). Removed from 4 adjective files, added to 4 generalbank files. Updated search index (both files). Manifest updated: adjectivebank 108→106, generalbank 185→186, curriculumWords 868→867, totalWords 3196→3195. Final adjective count: 106 entries."
  </action>
  <verify>
    1. `grep -r "lieblings-_adj" vocabulary/` returns zero matches
    2. `grep -r "lieblings-_expr" vocabulary/` returns matches in: core/de/generalbank.json, dictionary/de/generalbank.json, translations/de-nb/generalbank.json, translations/de-en/generalbank.json, dictionary/de/search-index.pretty.json, dictionary/de/search-index.json
    3. All modified JSON files parse without error
    4. manifest.json shows `"adjectivebank.json": 106` and `"generalbank.json": 186`
    5. Count entries in core/de/adjectivebank.json: `node -e "const d=JSON.parse(require('fs').readFileSync('vocabulary/core/de/adjectivebank.json','utf8')); const keys=Object.keys(d).filter(k=>k!=='_metadata'); console.log('Adjective entries:', keys.length)"` outputs 106
    6. Count entries in core/de/generalbank.json: `node -e "const d=JSON.parse(require('fs').readFileSync('vocabulary/core/de/generalbank.json','utf8')); const keys=Object.keys(d).filter(k=>k!=='_metadata'); console.log('General entries:', keys.length)"` outputs 186
  </verify>
  <done>
    lieblings-_adj appears in zero files. lieblings-_expr exists in generalbank (core, dictionary, translations nb+en, both search indexes). Manifest counts are correct: 106 adjectives, 186 general. 106 adjective entries remain in the core bank.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full validation and confirm clean state</name>
  <files>
    vocabulary/schema/adjective.schema.json
    vocabulary/core/de/adjectivebank.json
  </files>
  <action>
    **Step 1 — Install ajv for schema validation.**

    Run: `npm install --save-dev ajv ajv-formats`

    **Step 2 — Create a one-off validation script.**

    Create `scripts/validate-adjectives.js` (ESM) that:

    ```javascript
    import Ajv2020 from 'ajv/dist/2020.js';
    import { readFileSync } from 'fs';

    const coreSchema = JSON.parse(readFileSync('vocabulary/schema/core-word.schema.json', 'utf8'));
    const adjSchema = JSON.parse(readFileSync('vocabulary/schema/adjective.schema.json', 'utf8'));
    const adjBank = JSON.parse(readFileSync('vocabulary/core/de/adjectivebank.json', 'utf8'));

    const ajv = new Ajv2020({ strict: false, allErrors: true });
    ajv.addSchema(coreSchema, 'core-word.schema.json');
    ajv.addSchema(adjSchema);

    const validate = ajv.getSchema('https://papertek.no/schemas/vocabulary/adjective.schema.json');
    if (!validate) { console.error('Schema not found'); process.exit(1); }

    const valid = validate(adjBank);
    if (valid) {
      const count = Object.keys(adjBank).filter(k => k !== '_metadata').length;
      console.log(`PASS: All ${count} adjective entries validate against schema`);
      process.exit(0);
    } else {
      console.error('FAIL: Validation errors:');
      console.error(JSON.stringify(validate.errors, null, 2));
      process.exit(1);
    }
    ```

    Run: `node scripts/validate-adjectives.js`
    Expected output: `PASS: All 106 adjective entries validate against schema`

    **Step 3 — Run semantic checks.**

    Write a quick Node script (or inline) that:
    - Loads core/de/adjectivebank.json
    - For each entry, verifies `_id` matches the object key
    - Verifies no duplicate `word` values
    - Verifies `type` is `"adj"` for all entries
    - Reports any anomalies

    If any issues found, fix them and document in commit message. Based on research, expect zero issues.

    **Step 4 — Final commit.**

    Commit message: `feat(03-bank-cleanup): add adjective validation script and confirm clean bank`
    Include in commit body: "Added scripts/validate-adjectives.js for ajv-based schema validation. All 106 adjective entries pass validation with translations optional. Semantic checks confirm: zero duplicate words, all _id/key matches, all type=adj. Phase 3 complete: bank reduced from 108→106 entries (removed beste_adj, moved Lieblings- to generalbank)."
  </action>
  <verify>
    1. `node scripts/validate-adjectives.js` exits with code 0 and prints "PASS: All 106 adjective entries validate against schema"
    2. Semantic checks pass with zero anomalies
    3. `npm install` completes without error (ajv in devDependencies)
  </verify>
  <done>
    Schema validation passes on all 106 adjective entries. Semantic checks pass (no duplicates, no _id mismatches, no wrong types). ajv is installed and validation script is committed. Phase 3 bank cleanup is complete.
  </done>
</task>

</tasks>

<verification>
Run these checks to confirm the phase is fully complete:

1. **Schema validation:** `node scripts/validate-adjectives.js` → PASS on all 106 entries
2. **No orphaned references:** `grep -r "beste_adj" vocabulary/` → zero matches
3. **No orphaned references:** `grep -r "lieblings-_adj" vocabulary/` → zero matches
4. **Lieblings- reclassified:** `grep -r "lieblings-_expr" vocabulary/` → matches in 6 generalbank/search files
5. **Manifest accuracy:** `node -e "const m=JSON.parse(require('fs').readFileSync('vocabulary/dictionary/de/manifest.json','utf8')); console.log('adj:', m._metadata.files['adjectivebank.json'], 'gen:', m._metadata.files['generalbank.json'])"` → adj: 106, gen: 186
6. **All JSON valid:** Parse every modified file without error
7. **Entry count:** Core adjective bank has exactly 106 entries (excluding _metadata)
</verification>

<success_criteria>
1. `node scripts/validate-adjectives.js` produces zero schema validation errors on all 106 remaining adjective entries
2. `beste_adj` does not appear in any file under `vocabulary/`
3. `lieblings-_adj` does not appear in any file under `vocabulary/`; `lieblings-_expr` appears in generalbank core, dictionary, nb+en translations, and both search index files
4. `vocabulary/dictionary/de/manifest.json` shows adjectivebank: 106 and generalbank: 186
5. All modified JSON files parse without error
</success_criteria>

<output>
After completion, create `.planning/phases/03-bank-cleanup/03-01-SUMMARY.md`
</output>
