---
phase: 16-data-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-02
files_modified:
  - vocabulary/core/de/manifest.json
  - vocabulary/dictionary/de/manifest.json
  - scripts/fix-manifest-counts.js
autonomous: true
requirements:
  - DATA-04

must_haves:
  truths:
    - "Core manifest generalbank count matches actual generalbank entry count (186)"
    - "Core manifest nounbank count matches actual nounbank entry count (331)"
    - "Core manifest verbbank count matches actual verbbank entry count (148)"
    - "Dict manifest generalbank count matches actual dict generalbank entry count (673)"
    - "Dict manifest nounbank count matches actual dict nounbank entry count (1641)"
    - "Dict manifest verbbank count matches actual dict verbbank entry count (679)"
    - "All existing validation scripts pass after manifest fix"
  artifacts:
    - path: "scripts/fix-manifest-counts.js"
      provides: "Audit-trail script for manifest count corrections"
    - path: "vocabulary/core/de/manifest.json"
      provides: "Corrected core manifest with accurate file counts"
      contains: "_metadata"
    - path: "vocabulary/dictionary/de/manifest.json"
      provides: "Corrected dict manifest with accurate file counts"
      contains: "_metadata"
  key_links:
    - from: "scripts/fix-manifest-counts.js"
      to: "vocabulary/core/de/manifest.json"
      via: "count entries and update"
      pattern: "readFileSync.*manifest"
    - from: "scripts/fix-manifest-counts.js"
      to: "vocabulary/dictionary/de/manifest.json"
      via: "count entries and update"
      pattern: "readFileSync.*manifest"
---

<objective>
Fix manifest counts so they match actual entry counts in each bank file. Both core and dict manifests currently have stale/wrong counts for generalbank, nounbank, and verbbank.

Purpose: DATA-04 requires manifest counts to reflect reality. Currently core manifest shows generalbank:185 (actual:186), nounbank:332 (actual:331), verbbank:149 (actual:148). Dict manifest is far worse — its counts match the core bank sizes, not the dict bank sizes (generalbank:186 vs actual:673, nounbank:331 vs actual:1641, verbbank:148 vs actual:679).
Output: Corrected manifests + audit-trail script + full validation pass.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-data-fixes/16-CONTEXT.md
@.planning/phases/16-data-fixes/16-01-SUMMARY.md
@.planning/phases/16-data-fixes/16-02-SUMMARY.md
@vocabulary/core/de/manifest.json
@vocabulary/dictionary/de/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create manifest count fix script and apply fixes</name>
  <files>
    scripts/fix-manifest-counts.js
    vocabulary/core/de/manifest.json
    vocabulary/dictionary/de/manifest.json
  </files>
  <action>
Create `scripts/fix-manifest-counts.js` (ESM) that programmatically counts entries in every bank file and updates both manifests.

**Approach** (per user decision: Claude determines safest approach):
1. Read each bank file referenced in the manifest's `_metadata.files` object
2. Count actual entries: `Object.keys(data).filter(k => k !== '_metadata').length`
3. Compare declared count to actual count
4. If mismatch, update the declared count to actual
5. Also recompute `_metadata.totalWords` as the sum of all file counts
6. For dict manifest, also recompute the top-level `totalWords` field (sum of all dict bank entry counts)

**Core manifest fixes** (expected mismatches based on audit):
- `generalbank.json`: 185 → 186
- `nounbank.json`: 332 → 331
- `verbbank.json`: 149 → 148
- Recalculate `totalWords` accordingly

**Dict manifest fixes** (expected mismatches — these are severe, counts match core not dict):
- `generalbank.json`: 186 → 673
- `nounbank.json`: 331 → 1641
- `verbbank.json`: 148 → 679
- Also verify adjectivebank, articlesbank, numbersbank, phrasesbank, pronounsbank counts are correct
- Recalculate `_metadata.totalWords` from sum of all `_metadata.files` values
- Recalculate top-level `totalWords` from sum of all actual dict bank entry counts
- Verify `curriculumWords` and `dictionaryOnlyWords` — these may also need recalculation. curriculumWords = count of entries with `curriculum: true` across all dict banks. dictionaryOnlyWords = totalWords - curriculumWords.

**Safety**: The script should NOT hardcode expected values. It should dynamically count from the actual data files. This makes it safe to run at any time as a health check.

**Script pattern**: ESM, read-modify-write, console.log a before/after comparison table for each manifest showing old vs new counts per bank, highlighting any changes.

Also update `_metadata.generatedAt` to current ISO timestamp when making changes.

Run the script: `node scripts/fix-manifest-counts.js`
  </action>
  <verify>
Run: `node scripts/fix-manifest-counts.js`
Then full validation:
- `npm run validate:nouns` — passes
- `npm run validate:nouns:dict` — passes
- `npm run validate:verbs` — passes
- `npm run validate:verbs:dict` — passes
- `npm run validate:all` — passes (or document any pre-existing failures)
- Verify: `npm run verify:integration` — passes (or document pre-existing issues)
  </verify>
  <done>
Core manifest counts match actual bank sizes for generalbank (186), nounbank (331), verbbank (148). Dict manifest counts match actual dict bank sizes for generalbank (673), nounbank (1641), verbbank (679). totalWords fields recalculated. All validation scripts pass. No regressions.
  </done>
</task>

</tasks>

<verification>
1. `node scripts/fix-manifest-counts.js` runs without error
2. Core manifest `_metadata.files` counts match actual entry counts for ALL banks
3. Dict manifest `_metadata.files` counts match actual entry counts for ALL dict banks
4. Core `totalWords` = sum of all core bank counts
5. Dict `totalWords` = sum of all dict bank counts
6. `npm run validate:all` passes
7. `npm run verify:integration` passes (or pre-existing issues documented)
</verification>

<success_criteria>
DATA-04 satisfied: core manifest counts (generalbank, nounbank, verbbank) match actual bank entry counts. Dict manifest also corrected. All validation passes. Manifest is a reliable source of truth for bank sizes.
</success_criteria>

<output>
After completion, create `.planning/phases/16-data-fixes/16-03-SUMMARY.md`
</output>
