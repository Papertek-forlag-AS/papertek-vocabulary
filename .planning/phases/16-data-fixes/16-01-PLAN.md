---
phase: 16-data-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vocabulary/core/de/nounbank.json
  - vocabulary/dictionary/de/nounbank.json
  - scripts/fix-noun-fields.js
autonomous: true
requirements:
  - DATA-01
  - DATA-05
  - DATA-06

must_haves:
  truths:
    - "morgenmensch_noun in dict nounbank has genus field set to 'm'"
    - "zusammenfassung_noun has top-level plural field 'Zusammenfassungen' in core and dict nounbank"
    - "hilfe_noun has top-level plural field 'Hilfen' in core and dict nounbank"
    - "leute_noun has a genus representation that is cleanly documented for plural-only nouns in both core and dict nounbank"
  artifacts:
    - path: "scripts/fix-noun-fields.js"
      provides: "Audit-trail script for noun field fixes"
    - path: "vocabulary/core/de/nounbank.json"
      provides: "Corrected noun entries"
      contains: "zusammenfassung_noun"
    - path: "vocabulary/dictionary/de/nounbank.json"
      provides: "Corrected dict noun entries"
      contains: "morgenmensch_noun"
  key_links:
    - from: "scripts/fix-noun-fields.js"
      to: "vocabulary/core/de/nounbank.json"
      via: "read-modify-write"
      pattern: "readFileSync.*nounbank"
    - from: "scripts/fix-noun-fields.js"
      to: "vocabulary/dictionary/de/nounbank.json"
      via: "read-modify-write"
      pattern: "readFileSync.*nounbank"
---

<objective>
Fix missing noun fields across core and dictionary nounbanks: add genus to morgenmensch_noun in dict, populate top-level plural for zusammenfassung_noun and hilfe_noun, and handle leute_noun genus for plural-only noun pattern.

Purpose: DATA-01, DATA-05, DATA-06 require all noun entries to have complete required fields. morgenmensch_noun is missing genus in the dict bank; two nouns have declension-derived plural data but no top-level plural field; leute_noun (plural-only) needs a genus representation.
Output: Corrected noun entries in both banks + audit-trail script.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-data-fixes/16-CONTEXT.md
@vocabulary/core/de/nounbank.json
@vocabulary/dictionary/de/nounbank.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create noun field fix script and apply fixes</name>
  <files>
    scripts/fix-noun-fields.js
    vocabulary/core/de/nounbank.json
    vocabulary/dictionary/de/nounbank.json
  </files>
  <action>
Create `scripts/fix-noun-fields.js` (ESM) as an audit-trail script that performs all three noun fixes in a single read-modify-write pass per bank file.

**Fix 1 — DATA-01: morgenmensch_noun genus in dict nounbank**
- The core nounbank already has `genus: "m"` on morgenmensch_noun
- The dict nounbank is MISSING the genus field on morgenmensch_noun
- Add `genus: "m"` to the dict nounbank entry for morgenmensch_noun
- Insert the genus field after the `word` field (consistent with core entry structure)
- Also verify all other required fields on morgenmensch_noun while editing: word, type, audio, _id, curriculum, cefr, frequency, cases — fix any other missing fields found

**Fix 2 — DATA-05: Top-level plural for declension-based nouns**
- `zusammenfassung_noun`: has declension nominativ plural "die Zusammenfassungen" but NO top-level `plural` field
  - Add `plural: "Zusammenfassungen"` (bare noun, no article — match existing convention in nounbank)
  - Apply to BOTH core and dict nounbank
- `hilfe_noun`: has declension nominativ plural "die Hilfen" but NO top-level `plural` field
  - Add `plural: "Hilfen"` (bare noun, no article)
  - Apply to BOTH core and dict nounbank
- Insert the `plural` field after `genus` (consistent with existing entries that have plural)
- Check existing nounbank entries that DO have a `plural` field to confirm the format convention (bare noun without article)

**Fix 3 — DATA-06: leute_noun genus for plural-only noun**
- leute_noun has `declension_type: "plural-only"` and `plural: null` in core nounbank
- leute_noun has NO genus field in EITHER core or dict nounbank
- Per user decision: Claude picks cleanest approach for plural-only noun genus
- Recommended approach: add `genus: null` with a comment-style field `_genus_note: "Plural-only noun (die Leute) — no grammatical gender for singular"` to make the null intentional and documented
- However, JSON does not support comments. Better approach: add `genus: null` (explicit null signals intentional absence, distinguished from undefined/missing). The `declension_type: "plural-only"` already documents WHY genus is null.
- Apply `genus: null` to leute_noun in BOTH core and dict nounbank
- Also check the other plural-only nouns (`eltern_noun`, `ferien_noun`) — if they also lack genus, add `genus: null` to them too for consistency

**Entry-level verification** (per user decision):
- When touching morgenmensch_noun, also verify all other required fields on that entry in both banks
- Ensure no corruption of existing fields

**Script pattern**: Follow established audit-trail pattern from prior phases (ESM imports, read JSON, modify entries, write JSON with 2-space indent + trailing newline, console.log summary of changes).

Run the script after creation: `node scripts/fix-noun-fields.js`
  </action>
  <verify>
Run: `node scripts/fix-noun-fields.js`
Then verify with spot checks:
- `node -e 'import("fs").then(fs => { const d = JSON.parse(fs.readFileSync("vocabulary/dictionary/de/nounbank.json","utf8")); console.log("morgenmensch genus:", d.morgenmensch_noun.genus) })'` should print `m`
- Run existing validation: `npm run validate:nouns` and `npm run validate:nouns:dict` — both should pass without new errors
  </verify>
  <done>
morgenmensch_noun has genus:"m" in dict nounbank. zusammenfassung_noun and hilfe_noun have top-level plural fields in both banks. leute_noun has genus:null in both banks. All plural-only nouns consistently have genus:null. Existing validation passes.
  </done>
</task>

</tasks>

<verification>
1. `node scripts/fix-noun-fields.js` runs without error
2. `npm run validate:nouns` passes (core nounbank)
3. `npm run validate:nouns:dict` passes (dict nounbank)
4. Spot-check: morgenmensch_noun dict entry has genus:"m"
5. Spot-check: zusammenfassung_noun has plural:"Zusammenfassungen" in both banks
6. Spot-check: hilfe_noun has plural:"Hilfen" in both banks
7. Spot-check: leute_noun has genus:null in both banks
</verification>

<success_criteria>
All 3 noun data requirements (DATA-01, DATA-05, DATA-06) satisfied. Dict morgenmensch has genus. Both declension-based nouns have top-level plural populated. All plural-only nouns have explicit genus:null. AJV validation passes for both core and dict nounbanks.
</success_criteria>

<output>
After completion, create `.planning/phases/16-data-fixes/16-01-SUMMARY.md`
</output>
