---
phase: 22-api-updates
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - scripts/validate-nouns.js
  - scripts/validate-verbs.js
  - scripts/validate-adjectives.js
  - scripts/verify-integration.js
  - package.json
autonomous: true
requirements: [API-01, API-02, API-03, API-04, API-05]

must_haves:
  truths:
    - "vocabulary/core/de/ directory no longer exists (audio files moved to banks/de/audio/)"
    - "merge-banks.js and merge-translations.js scripts no longer exist"
    - "npm run validate:all passes against vocabulary/banks/de/ paths"
    - "No active scripts reference vocabulary/core/de/ or vocabulary/dictionary/de/ for data reads"
    - "Audio files accessible at vocabulary/banks/de/audio/"
  artifacts:
    - path: "scripts/validate-nouns.js"
      provides: "Noun validation against banks/de/"
      contains: "vocabulary/banks"
    - path: "scripts/validate-verbs.js"
      provides: "Verb validation against banks/de/"
      contains: "vocabulary/banks"
    - path: "scripts/validate-adjectives.js"
      provides: "Adjective validation against banks/de/"
      contains: "vocabulary/banks"
    - path: "scripts/verify-integration.js"
      provides: "Integration verification against banks/de/"
      contains: "vocabulary/banks"
    - path: "package.json"
      provides: "Clean npm scripts (no merge:banks, merge:translations)"
  key_links:
    - from: "scripts/validate-nouns.js"
      to: "vocabulary/banks/de/nounbank.json"
      via: "default bank path"
      pattern: "vocabulary/banks/de/nounbank"
    - from: "scripts/verify-integration.js"
      to: "vocabulary/banks/de/"
      via: "bank data paths"
      pattern: "vocabulary/banks/de"
---

<objective>
Delete old directories and migration scripts, update active validation/verification scripts to read from vocabulary/banks/de/, and move audio files to the new location.

Purpose: Complete the data migration by removing stale paths (vocabulary/core/de/) and one-time migration tools (merge-banks.js, merge-translations.js). Update the active validator and verification scripts so `npm run validate:all` works against the new single-bank structure. This is the cleanup locked in CONTEXT.md.

Output: Clean repository with no stale core/ or dictionary/ references in active code, working validators, and audio files at their new location.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-api-updates/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old directories and migration scripts</name>
  <files>
    vocabulary/core/de/ (DELETE — except move audio/ to banks/de/audio/ first)
    scripts/merge-banks.js (DELETE)
    scripts/merge-translations.js (DELETE)
    package.json
  </files>
  <action>
1. **Move audio files** before deleting core/de/:
   - `vocabulary/core/de/audio/` contains mp3 files used by the API's `audioUrl` field
   - Copy (not move — safer) the entire audio directory: `cp -r vocabulary/core/de/audio vocabulary/banks/de/audio`
   - Verify the copy: count files in both directories, ensure they match
   - The API handlers (updated in Plan 22-01) now reference `/shared/vocabulary/banks/${lang}/audio/` for audio URLs

2. **Delete vocabulary/core/de/** directory:
   - `rm -rf vocabulary/core/de/`
   - Check if `vocabulary/core/` is now empty — if so, delete it too
   - This satisfies BANK-06 (old core directory removed) per CONTEXT.md locked decision

3. **Delete one-time migration scripts** (per CONTEXT.md locked decision):
   - `rm scripts/merge-banks.js`
   - `rm scripts/merge-translations.js`
   - These are preserved in git history — no data loss

4. **Update package.json** — remove npm scripts for deleted tools:
   - Remove `"merge:banks": "node scripts/merge-banks.js"` line
   - Remove `"merge:translations": "node scripts/merge-translations.js"` line
   - Keep all other scripts (validate:*, verify:integration, build:search-index, dev)

5. **One-time scripts with old paths** — these are audit-trail artifacts that operated on the old structure. They are NOT actively run and reference files that no longer exist. Leave them in place — they serve as historical documentation and are harmless (git history preserves context). The scripts are: fix-manifest-counts.js, fix-verb-fields.js, fix-noun-fields.js, fix-validation.js, sync-nouns.js, sync-perfektum.js, sync-preteritum.js, add-declension.js, add-perfektum.js, verify-audit03.js, add-audit-flags.js, check-audit.js.
  </action>
  <verify>
Run `ls vocabulary/core/de/ 2>/dev/null` — should fail (directory gone).
Run `ls vocabulary/banks/de/audio/*.mp3 | wc -l` — should match the pre-move count.
Run `ls scripts/merge-banks.js scripts/merge-translations.js 2>/dev/null` — should return nothing.
Run `node -e "const p = JSON.parse(require('fs').readFileSync('package.json','utf8')); console.log('merge:banks' in p.scripts ? 'FAIL' : 'OK'); console.log('merge:translations' in p.scripts ? 'FAIL' : 'OK')"` — both should print OK.
  </verify>
  <done>
vocabulary/core/de/ deleted, audio files at vocabulary/banks/de/audio/, merge scripts removed, package.json cleaned.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update active validators and verify-integration to use banks/de/</name>
  <files>
    scripts/validate-nouns.js
    scripts/validate-verbs.js
    scripts/validate-adjectives.js
    scripts/verify-integration.js
  </files>
  <action>
1. **scripts/validate-nouns.js**:
   - Change default path from `'vocabulary/core/de/nounbank.json'` to `'vocabulary/banks/de/nounbank.json'`
   - Keep the `NOUN_BANK` env var override for flexibility
   - This is a single string replacement on line 4

2. **scripts/validate-verbs.js**:
   - Change default path from `'vocabulary/core/de/verbbank.json'` to `'vocabulary/banks/de/verbbank.json'`
   - Keep the `VERB_BANK` env var override
   - Single string replacement on line 4

3. **scripts/validate-adjectives.js**:
   - Change path from `'vocabulary/core/de/adjectivebank.json'` to `'vocabulary/banks/de/adjectivebank.json'`
   - Single string replacement on line 6

4. **scripts/verify-integration.js**:
   - This script has multiple old path references. Update ALL of them:
     - `vocabulary/core/de/verbbank.json` → `vocabulary/banks/de/verbbank.json`
     - `vocabulary/core/de/nounbank.json` → `vocabulary/banks/de/nounbank.json`
     - `vocabulary/dictionary/de/verbbank.json` → `vocabulary/banks/de/verbbank.json`
     - `vocabulary/dictionary/de/nounbank.json` → `vocabulary/banks/de/nounbank.json`
     - `vocabulary/dictionary/de/search-index.json` → `vocabulary/banks/de/search-index.json`
   - The script currently reads BOTH core and dict banks separately and compares them. Since they're now merged into one file, the script needs restructuring:
     - Instead of reading separate core+dict banks, read the single merged bank
     - Instead of comparing core entries exist in dict, verify curriculum entries exist in manifest
     - The script's purpose is integration health check — adapt checks to the new single-bank reality
     - Key checks to preserve: search index entry count (3454), verb pp field coverage (144), noun declension type coverage (331), feature flag correctness
     - Remove checks that compared core-to-dict (no longer meaningful)
     - Add check: manifest curriculum IDs actually exist in bank files
   - Read the current verify-integration.js fully before editing to understand all its checks
  </action>
  <verify>
Run `npm run validate:nouns` — should pass (0 errors or expected baseline).
Run `npm run validate:verbs` — should pass.
Run `npm run validate:adjectives` — should pass.
Run `npm run verify:integration` — should pass with updated checks.
Run `npm run validate:all` — full chain should pass.
Grep all 4 scripts for 'vocabulary/core' and 'vocabulary/dictionary' — should return 0 matches.
  </verify>
  <done>
All 4 active validator/verification scripts read from vocabulary/banks/de/. `npm run validate:all` passes. No active script references old core/ or dictionary/ paths.
  </done>
</task>

</tasks>

<verification>
1. `vocabulary/core/de/` does not exist
2. `vocabulary/banks/de/audio/` exists with correct mp3 file count
3. `scripts/merge-banks.js` and `scripts/merge-translations.js` do not exist
4. `package.json` has no merge:banks or merge:translations scripts
5. `npm run validate:all` passes (validators read from banks/de/)
6. No active API or validation scripts reference vocabulary/core/de/ or vocabulary/dictionary/de/
</verification>

<success_criteria>
- Old core/de/ directory deleted, audio files preserved at banks/de/audio/
- Migration scripts removed (preserved in git history)
- Active validators updated and passing
- verify-integration.js adapted for single-bank structure and passing
- `npm run validate:all` succeeds end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/22-api-updates/22-02-SUMMARY.md`
</output>
