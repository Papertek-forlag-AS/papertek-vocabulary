---
phase: 22-api-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/vocab/v1/core/[language].js
  - api/vocab/v1/manifest.js
  - api/vocab/v2/search/[language].js
  - api/vocab/v2/lookup/[language]/[wordId].js
autonomous: true
requirements: [API-01, API-02, API-03, API-04, API-05]

must_haves:
  truths:
    - "v1 core handler returns only curriculum entries from vocabulary/banks/de/"
    - "v1 core handler response shape is unchanged (banks list, _metadata, per-bank word objects)"
    - "v1 manifest handler reads language info from vocabulary/banks/de/ manifest"
    - "v2 search handler loads search index from vocabulary/banks/de/search-index.json"
    - "v2 lookup handler loads word entries from vocabulary/banks/de/{bankname}.json"
    - "v2 lookup handler has no -dict/ fallback code for translations"
  artifacts:
    - path: "api/vocab/v1/core/[language].js"
      provides: "v1 core handler reading from banks/de/ with manifest filtering"
      contains: "vocabulary/banks"
    - path: "api/vocab/v1/manifest.js"
      provides: "v1 manifest handler reading from banks/de/"
      contains: "vocabulary/banks"
    - path: "api/vocab/v2/search/[language].js"
      provides: "v2 search handler reading from banks/de/search-index.json"
      contains: "vocabulary/banks"
    - path: "api/vocab/v2/lookup/[language]/[wordId].js"
      provides: "v2 lookup handler reading from banks/de/"
      contains: "vocabulary/banks"
  key_links:
    - from: "api/vocab/v1/core/[language].js"
      to: "vocabulary/banks/de/manifest.json"
      via: "manifest curriculum IDs filter"
      pattern: "manifest\\.banks.*ids"
    - from: "api/vocab/v2/search/[language].js"
      to: "vocabulary/banks/de/search-index.json"
      via: "search index path"
      pattern: "banks.*search-index"
---

<objective>
Migrate all 4 API handlers that still reference old data paths (vocabulary/core/ and vocabulary/dictionary/) to read exclusively from the new single-bank structure at vocabulary/banks/.

Purpose: This is the core requirement of Phase 22 — the API layer must read from the consolidated bank structure created in Phases 20-21. The v1 core handler additionally needs manifest-based curriculum filtering since the merged banks contain all entries (curriculum + dictionary-only).

Output: 4 updated API handler files reading from vocabulary/banks/ with no references to vocabulary/core/ or vocabulary/dictionary/.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-bank-manifest-consolidation/20-01-SUMMARY.md
@.planning/phases/21-translation-consolidation/21-01-SUMMARY.md

# Key data files
@vocabulary/banks/de/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate v1 core and manifest handlers to banks/de/</name>
  <files>
    api/vocab/v1/core/[language].js
    api/vocab/v1/manifest.js
  </files>
  <action>
**v1 core handler** (`api/vocab/v1/core/[language].js`):

1. Change the data source path from `vocabulary/core/${lang}` to `vocabulary/banks/${lang}`.
   - Line 58: `const langPath = path.join(vocabBase, 'banks', normalizedLang);` (was `'core'`)
   - Update the debug mode block similarly: replace `'core'` references with `'banks'`

2. Add manifest-based curriculum filtering. The merged banks contain ALL entries (curriculum + dictionary-only). The v1 core endpoint must return only curriculum entries.
   - After resolving `langPath`, load the manifest: `const manifest = JSON.parse(fs.readFileSync(path.join(langPath, 'manifest.json'), 'utf-8'));`
   - Build a Set of curriculum IDs per bank from `manifest.banks[bankName].ids`

3. When returning a single bank (`?bank=verbbank`):
   - After reading the bank JSON, filter to only include entries whose keys appear in `manifest.banks[bankName].ids`
   - Reconstruct the object with only curriculum entries (plus `_metadata` if present)

4. When returning all banks combined:
   - For each bank file, after reading, filter entries by manifest IDs before adding to `combined`
   - The response shape stays identical: `{ _metadata: { language, requestedAt, banks[] }, bankName: { wordId: {...}, ... }, ... }`

5. Response shape preservation (API-02):
   - The curriculum entries in the merged bank already contain the same fields as old core bank entries (word, _id, audio, conjugations, cases, etc.)
   - The merged bank also has additional fields (curriculum, cefr, frequency, tags) from the dict bank — per user's discretion, these extra fields are acceptable (internal-only API, not strict byte-for-byte)
   - Do NOT strip extra fields — enriched data is fine per CONTEXT.md Claude's Discretion

**v1 manifest handler** (`api/vocab/v1/manifest.js`):

1. Core languages section (lines 26-63): Currently reads from `vocabulary/core/` and `vocabulary/core/${lang}/manifest.json`.
   - Change `corePath` to: `path.join(vocabPath, 'banks')` (line 26)
   - The manifest at `vocabulary/banks/de/manifest.json` has a different structure than the old core manifest. The old manifest had `_metadata.targetLanguageName`, `_metadata.version`, `_metadata.totalWords`, `_metadata.files`.
   - Adapt to read from new manifest structure: use `manifest.summary.curriculumWords` for totalWords (since v1 shows curriculum only), infer version from `_metadata.generatedAt`, list bank files from `manifest.banks` keys, keep `targetLanguageName` hardcoded or derive from lang code (`de` -> "German", `es` -> "Spanish", `fr` -> "French").
   - Audio path: still at `vocabulary/banks/${lang}/audio` — update `audioPath` to use `banksPath` instead of `corePath`
   - Endpoint: keep as `/api/vocab/v1/core/${lang}` (external URL unchanged)
   - The `baseUrl` for audio: keep as `/shared/vocabulary/core/${lang}/audio` — this is the public URL for static files; it depends on the actual file location on disk. Since we're also moving core/de to banks/de, the audio files move too. But wait — looking at the actual directory, `vocabulary/core/de/audio/` has the audio files. We are deleting `vocabulary/core/de/` in Plan 22-02. So we need the audio URL to point to the new location. However, Vercel serves static files from the filesystem, so the audio URL should become `/shared/vocabulary/banks/${lang}/audio`. BUT, we also need to check if any external clients depend on the old `/shared/vocabulary/core/` URL path. Per CONTEXT.md, this is internal-only and Claude has discretion. Update the audio baseUrl to `/shared/vocabulary/banks/${lang}/audio` so it matches the actual file location after cleanup.

2. Dictionary section (lines 116-136): Currently reads from `vocabulary/dictionary/${lang}/manifest.json` — this directory was DELETED in Phase 21-02.
   - Replace with reading from `vocabulary/banks/${lang}/manifest.json`
   - Map fields: `totalWords` from `manifest.summary.totalWords`, `curriculumWords` from `manifest.summary.curriculumWords`, `dictionaryOnlyWords` from `manifest.summary.dictionaryOnlyWords`
   - Keep `searchEndpoint` and `lookupEndpoint` URLs unchanged

3. Translation packs section (lines 66-74): Already reads from `vocabulary/translations/` — no change needed (already correct after Phase 21).
  </action>
  <verify>
Run `node -e "import('./api/vocab/v1/core/[language].js').then(() => console.log('v1 core: syntax OK')).catch(e => console.error(e.message))"` to check syntax.
Run `node -e "import('./api/vocab/v1/manifest.js').then(() => console.log('v1 manifest: syntax OK')).catch(e => console.error(e.message))"` to check syntax.
Grep both files for 'vocabulary/core' and 'vocabulary/dictionary' — should return 0 matches (only 'vocabulary/banks' references).
  </verify>
  <done>
v1 core handler reads from vocabulary/banks/{lang}/ and filters by manifest curriculum IDs. v1 manifest handler reads all language/dict info from vocabulary/banks/ manifests. Neither file references vocabulary/core/ or vocabulary/dictionary/ in data-reading paths.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate v2 search and lookup handlers to banks/de/</name>
  <files>
    api/vocab/v2/search/[language].js
    api/vocab/v2/lookup/[language]/[wordId].js
  </files>
  <action>
**v2 search handler** (`api/vocab/v2/search/[language].js`):

1. Update `loadSearchIndex()` function (line 32-37):
   - Change path from `path.join(vocabBase, 'dictionary', langCode, 'search-index.json')` to `path.join(vocabBase, 'banks', langCode, 'search-index.json')`
   - This is the only path change needed — the rest of the search logic operates on the loaded index object and is path-agnostic

**v2 lookup handler** (`api/vocab/v2/lookup/[language]/[wordId].js`):

1. Update direct bank lookup path (line 132):
   - Change from `path.join(vocabBase, 'dictionary', langCode, ...)` to `path.join(vocabBase, 'banks', langCode, ...)`

2. Update `findWordInAllBanks()` function (lines 80-93):
   - Change `dictPath` from `path.join(vocabBase, 'dictionary', langCode)` to `path.join(vocabBase, 'banks', langCode)`
   - Rename variable from `dictPath` to `banksPath` for clarity

3. Audio URL (line 182): Currently `/shared/vocabulary/core/${langCode}/audio/${entry.audio}` — update to `/shared/vocabulary/banks/${langCode}/audio/${entry.audio}` to match the new file location after core/ is deleted in Plan 02.

4. Translation path (line 162): Already reads from `vocabulary/translations/${pair}/` — correct, no change needed (Phase 21 consolidated this).

5. The response shape and grammar features logic are all data-driven from the entry object — no changes needed. The merged bank entries have all the same fields (and more) than the old dict bank entries.
  </action>
  <verify>
Run `node -e "import('./api/vocab/v2/search/[language].js').then(() => console.log('v2 search: syntax OK')).catch(e => console.error(e.message))"` to check syntax.
Run `node -e "import('./api/vocab/v2/lookup/[language]/[wordId].js').then(() => console.log('v2 lookup: syntax OK')).catch(e => console.error(e.message))"` to check syntax.
Grep both files for 'vocabulary/dictionary' — should return 0 matches.
Grep lookup handler for 'vocabulary/core' — should return 0 matches (audio URL updated).
  </verify>
  <done>
v2 search handler loads search index from vocabulary/banks/{lang}/search-index.json. v2 lookup handler loads word data from vocabulary/banks/{lang}/ and has no dictionary/ or core/ path references. Audio URL points to banks/{lang}/audio/.
  </done>
</task>

</tasks>

<verification>
1. All 4 modified API handler files contain ZERO references to `vocabulary/core/` or `vocabulary/dictionary/` in their data-reading paths
2. v1 core handler correctly reads from `vocabulary/banks/` and filters by manifest IDs
3. v1 manifest handler reads core and dictionary info from `vocabulary/banks/` manifest
4. v2 search handler loads search index from `vocabulary/banks/{lang}/search-index.json`
5. v2 lookup handler loads entries from `vocabulary/banks/{lang}/`
6. All files pass basic syntax check (ESM import succeeds)
</verification>

<success_criteria>
- 4 API handlers updated, 0 references to old paths in api/ directory
- v1 core returns curriculum-only entries filtered by manifest
- v1 manifest returns correct language info from new manifest structure
- v2 search uses correct search index path
- v2 lookup uses correct bank path
- All handlers syntactically valid
</success_criteria>

<output>
After completion, create `.planning/phases/22-api-updates/22-01-SUMMARY.md`
</output>
