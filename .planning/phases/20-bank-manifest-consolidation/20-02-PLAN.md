---
phase: 20-bank-manifest-consolidation
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - scripts/build-search-index.js
  - vocabulary/banks/de/search-index.json
autonomous: true
requirements:
  - BANK-05

must_haves:
  truths:
    - "Search index is rebuilt from merged banks under vocabulary/banks/de/"
    - "Search index contains all 3454 entries (same count as before)"
    - "Verb entries in search index have pp field (past participle from merged bank's conjugations.perfektum.participle)"
    - "Translation lookup still works (curriculum from de-nb/de-en, dict-only from de-nb-dict/de-en-dict)"
  artifacts:
    - path: "vocabulary/banks/de/search-index.json"
      provides: "Search index rebuilt from merged banks"
      contains: "_meta"
    - path: "scripts/build-search-index.js"
      provides: "Updated search index builder reading from vocabulary/banks/de/"
      min_lines: 100
  key_links:
    - from: "scripts/build-search-index.js"
      to: "vocabulary/banks/de/*bank.json"
      via: "reads merged banks as data source"
      pattern: "vocabulary/banks/de"
    - from: "vocabulary/banks/de/search-index.json"
      to: "vocabulary/banks/de/*bank.json"
      via: "index entries reference bank entry IDs"
      pattern: "entries.*id"
---

<objective>
Update the search index builder to read from the new merged banks under `vocabulary/banks/de/` instead of `vocabulary/dictionary/de/`, then rebuild the search index.

Purpose: The search index must reflect the new single-bank location. Since merged banks now contain perfektum data directly (from deep-merged core fields), the script no longer needs a separate core verbbank read for the pp field.

Output: Updated build-search-index.js + rebuilt search-index.json under vocabulary/banks/de/.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-bank-manifest-consolidation/20-01-SUMMARY.md
@scripts/build-search-index.js
@vocabulary/banks/de/verbbank.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update search index builder for merged banks</name>
  <files>scripts/build-search-index.js</files>
  <action>
Update `scripts/build-search-index.js` to read from merged banks instead of dict banks:

1. **Change BASE path:**
   - Old: `const BASE = 'vocabulary/dictionary/de';`
   - New: `const BASE = 'vocabulary/banks/de';`

2. **Remove separate core verbbank read for pp field:**
   - Old: Reads `vocabulary/core/de/verbbank.json` separately to build `ppMap` for past participle lookup
   - New: The merged verbbank already has `conjugations.perfektum.participle` on curriculum verbs (deep-merged from core). Read pp directly from the merged verbbank entry:
     ```js
     // In buildEntry for verbs:
     const participle = entry?.conjugations?.perfektum?.participle;
     if (participle) indexEntry.pp = participle;
     ```
   - Delete the `coreVerbBank` import and `ppMap` construction block entirely

3. **Update header comment:**
   - Reference Phase 20 merged banks
   - Remove references to "dict banks" and "core verbbank (for pp field)"
   - Update translation lookup comment (still uses de-nb/de-en + de-nb-dict/de-en-dict for now — Phase 21 will consolidate translations)

4. **Output path:**
   - Old: writes to `${BASE}/search-index.json` which was `vocabulary/dictionary/de/search-index.json`
   - New: writes to `vocabulary/banks/de/search-index.json` (same BASE, new location)

5. **Keep everything else unchanged:**
   - Translation lookup logic stays the same (de-nb + de-nb-dict fallback)
   - Entry building logic stays the same (verb, noun, adj type-specific fields)
   - Sorting by id stays the same
   - Output format stays the same

6. **Update npm script:**
   - `build:search-index` already points to `node scripts/build-search-index.js` — no change needed since the script itself handles the path change internally

**Key constraint:** Do NOT change translation paths yet. Phase 21 will consolidate translations. For now, the search index builder still reads from `vocabulary/translations/de-nb/`, `de-en/`, `de-nb-dict/`, `de-en-dict/`.
  </action>
  <verify>Run `node scripts/build-search-index.js` — outputs "Total entries: 3454" and verb/pp counts.</verify>
  <done>Search index builder reads from vocabulary/banks/de/ instead of vocabulary/dictionary/de/. No separate core verbbank read. Script runs successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Verify rebuilt search index correctness</name>
  <files>vocabulary/banks/de/search-index.json</files>
  <action>
After rebuilding the search index, verify it matches the expected output:

1. **Total entry count:** Must be 3454 (matching dict manifest total and previous index)

2. **Verb pp field verification:**
   - Load the new search index
   - Count verb entries with `pp` field — should be 144 (all curriculum verbs with Perfektum)
   - Spot-check: `anfangen_verb` should have `pp: "angefangen"`, `sein_verb` should have `pp: "gewesen"`

3. **Noun genus field verification:**
   - `mathematik_noun` should have `g: "f"` (was a core-exclusive field, now in merged bank)

4. **Curriculum flag verification:**
   - Count entries with `cur: true` — should match previous total (867 curriculum words from dict manifest)
   - Count entries with `cur: false` — should be 2587

5. **Compare old vs new index:**
   - Load `vocabulary/dictionary/de/search-index.json` (old, still exists)
   - Load `vocabulary/banks/de/search-index.json` (new)
   - Compare entry counts: must be identical (3454)
   - For each entry ID in old index, verify it exists in new index
   - Compare field values for a sample of entries (first 10) — all fields should match except potentially `generatedAt` timestamp in `_meta`

6. **If any discrepancy found:** Fix the search index builder and re-run.

Run these checks as inline Node.js commands or a small verification script.
  </action>
  <verify>All verification checks pass. New search index at vocabulary/banks/de/search-index.json has 3454 entries, 144 verbs with pp, correct curriculum counts.</verify>
  <done>Search index rebuilt from merged banks is identical in content to the previous dict-based index. All entry counts, field values, and curriculum flags match. BANK-05 satisfied.</done>
</task>

</tasks>

<verification>
1. `node scripts/build-search-index.js` completes without errors, outputs "Total entries: 3454"
2. `ls vocabulary/banks/de/search-index.json` exists
3. New search index has 3454 entries matching the old one
4. 144 verb entries have pp field
5. No references to `vocabulary/core/de/verbbank.json` remain in build-search-index.js
6. `npm run build:search-index` works
</verification>

<success_criteria>
- Search index rebuilt from merged banks under vocabulary/banks/de/
- Total entry count remains 3454
- All verb pp fields present (144 verbs)
- build-search-index.js no longer reads from vocabulary/core/de/ or vocabulary/dictionary/de/
- Index content matches old index (same entries, same fields, same values)
</success_criteria>

<output>
After completion, create `.planning/phases/20-bank-manifest-consolidation/20-02-SUMMARY.md`
</output>
