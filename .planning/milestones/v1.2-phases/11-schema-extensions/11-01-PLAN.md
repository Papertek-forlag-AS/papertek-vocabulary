---
phase: 11-schema-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vocabulary/schema/verb.schema.json
  - vocabulary/schema/noun.schema.json
  - vocabulary/grammar-features.json
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-02
  - SCHEMA-03
  - SCHEMA-04
  - SCHEMA-05

must_haves:
  truths:
    - "verb.schema.json accepts optional auxiliary, participle, auxiliary_note, dual_auxiliary, and modal_note fields on tenseConjugation without breaking existing presens/preteritum data"
    - "verb.schema.json accepts optional inseparable boolean on verbEntry without breaking any of the 148 existing verb entries"
    - "noun.schema.json accepts singular/plural nullable sub-objects inside caseEntry.forms alongside existing flat bestemt/ubestemt fields"
    - "noun.schema.json accepts optional weak_masculine boolean on nounEntry without breaking any of the 331 existing noun entries"
    - "grammar-features.json contains grammar_noun_declension and grammar_genitiv features under de.features with correct dependsOn relationship"
  artifacts:
    - path: "vocabulary/schema/verb.schema.json"
      provides: "Perfektum fields on tenseConjugation and inseparable flag on verbEntry"
      contains: "auxiliary"
    - path: "vocabulary/schema/noun.schema.json"
      provides: "Singular/plural sub-objects on caseEntry.forms and weak_masculine flag on nounEntry"
      contains: "weak_masculine"
    - path: "vocabulary/grammar-features.json"
      provides: "grammar_noun_declension and grammar_genitiv feature entries"
      contains: "grammar_noun_declension"
  key_links:
    - from: "vocabulary/grammar-features.json"
      to: "vocabulary/grammar-features.json"
      via: "grammar_genitiv.dependsOn references grammar_noun_declension"
      pattern: "dependsOn.*grammar_noun_declension"
    - from: "vocabulary/schema/noun.schema.json"
      to: "vocabulary/schema/noun.schema.json"
      via: "caseEntry.forms has both legacy flat keys and new singular/plural sub-objects"
      pattern: "singular.*plural"
---

<objective>
Extend verb and noun JSON schemas to accept Perfektum conjugation fields and 4-case noun declension fields; register two new grammar features for progressive disclosure.

Purpose: Phase 11 is the schema foundation for v1.2. Without these schema changes, Phase 12 (audit flags), Phase 13 (Perfektum data), and Phase 14 (noun declension data) cannot write data that passes validation.

Output: Three updated JSON files — verb schema, noun schema, grammar features config.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-schema-extensions/11-CONTEXT.md
@.planning/phases/11-schema-extensions/11-RESEARCH.md
@vocabulary/schema/verb.schema.json
@vocabulary/schema/noun.schema.json
@vocabulary/grammar-features.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend verb schema with Perfektum and inseparable fields</name>
  <files>vocabulary/schema/verb.schema.json</files>
  <action>
Edit `vocabulary/schema/verb.schema.json` to add two sets of changes:

**1a. Add Perfektum-related fields to `tenseConjugation` (inside `$defs.tenseConjugation.properties`):**

Add these 5 optional properties alongside the existing `intro`, `feature`, and `former`:

```json
"auxiliary": {
  "type": "string",
  "description": "Auxiliary verb for Perfektum: haben, sein, or both",
  "enum": ["haben", "sein", "both"]
},
"participle": {
  "type": "string",
  "description": "Past participle form (e.g., gemacht, gegangen)"
},
"auxiliary_note": {
  "type": "object",
  "description": "Object keyed by auxiliary explaining when each is used (dual-auxiliary verbs only)",
  "additionalProperties": { "type": "string" }
},
"dual_auxiliary": {
  "type": "boolean",
  "description": "True for verbs that use both haben and sein depending on usage"
},
"modal_note": {
  "type": "string",
  "description": "Explains Ersatzinfinitiv behavior (modal verbs only)"
}
```

Do NOT add any of these to the `required` array. The only required field on tenseConjugation remains `"former"`.

**1b. Add `inseparable` flag to `verbEntry` (inside `$defs.verbEntry.properties`):**

Add after the existing `conjugations` property:

```json
"inseparable": {
  "type": "boolean",
  "description": "True for verbs with inseparable prefixes (be-, er-, ge-, ent-, emp-, miss-, ver-, zer-) that never add ge- in participle"
}
```

Do NOT add `inseparable` to the `required` array on verbEntry.

**Anti-patterns to avoid:**
- Do NOT add `additionalProperties: false` to any object.
- Do NOT change the existing `"required": ["former"]` on tenseConjugation.
- Do NOT change the existing `"required": ["translations"]` on verbEntry.
  </action>
  <verify>
1. Run `node -e "const s = JSON.parse(require('fs').readFileSync('vocabulary/schema/verb.schema.json','utf8')); const tc = s.$defs.tenseConjugation.properties; console.log('auxiliary:', !!tc.auxiliary, 'participle:', !!tc.participle, 'auxiliary_note:', !!tc.auxiliary_note, 'dual_auxiliary:', !!tc.dual_auxiliary, 'modal_note:', !!tc.modal_note); console.log('inseparable:', !!s.$defs.verbEntry.properties.inseparable); console.log('required unchanged:', JSON.stringify(s.$defs.tenseConjugation.required));"` — all fields exist, required is still `["former"]`.
2. Run `node -e "const Ajv = require('ajv/dist/2020.js').default; const ajv = new Ajv({strict:false,allErrors:true}); const core = JSON.parse(require('fs').readFileSync('vocabulary/schema/core-word.schema.json','utf8')); const schema = JSON.parse(require('fs').readFileSync('vocabulary/schema/verb.schema.json','utf8')); const data = JSON.parse(require('fs').readFileSync('vocabulary/core/de/verbbank.json','utf8')); ajv.addSchema(core,'core-word.schema.json'); const v = ajv.compile(schema); console.log('valid:', v(data)); if(!v(data)) console.log(v.errors);"` — existing verbbank data still validates.
  </verify>
  <done>verb.schema.json has 5 new optional Perfektum fields on tenseConjugation (auxiliary, participle, auxiliary_note, dual_auxiliary, modal_note) and 1 new optional inseparable boolean on verbEntry. All 148 existing verb entries still pass schema validation.</done>
</task>

<task type="auto">
  <name>Task 2: Extend noun schema with case forms and weak_masculine fields</name>
  <files>vocabulary/schema/noun.schema.json</files>
  <action>
Edit `vocabulary/schema/noun.schema.json` to add two sets of changes:

**2a. Add `singular` and `plural` sub-objects to `caseEntry.forms` (inside `$defs.caseEntry.properties.forms.properties`):**

Add these two properties alongside the existing `bestemt`, `ubestemt`, `definite`, `indefinite` flat string fields:

```json
"singular": {
  "description": "Singular forms (null for plural-only nouns like Eltern)",
  "oneOf": [
    { "type": "null" },
    {
      "type": "object",
      "properties": {
        "definite": { "type": "string" },
        "indefinite": { "type": "string" }
      }
    }
  ]
},
"plural": {
  "description": "Plural forms (null for uncountable nouns)",
  "oneOf": [
    { "type": "null" },
    {
      "type": "object",
      "properties": {
        "definite": { "type": "string" },
        "indefinite": { "type": "string" }
      }
    }
  ]
}
```

**2b. Add `bestemt` and `ubestemt` as direct properties of `caseEntry` (inside `$defs.caseEntry.properties`):**

The research identified that real data has `bestemt`/`ubestemt` directly on `caseEntry` (not inside `forms`), but the current schema does not document this. Add these two properties to `caseEntry.properties` alongside `intro`, `feature`, `forms`:

```json
"bestemt": {
  "type": "string",
  "description": "Legacy flat definite form (pre-migration, directly on caseEntry)"
},
"ubestemt": {
  "type": "string",
  "description": "Legacy flat indefinite form (pre-migration, directly on caseEntry)"
}
```

This documents the actual data structure. Existing data uses these fields directly on caseEntry, not inside forms.

**2c. Add `weak_masculine` flag to `nounEntry` (inside `$defs.nounEntry.properties`):**

Add after the existing `cases` property:

```json
"weak_masculine": {
  "type": "boolean",
  "description": "True for n-Deklination masculine nouns that take -(e)n in all non-nominative singular cases"
}
```

Do NOT add `weak_masculine` to the `required` array on nounEntry.

**Anti-patterns to avoid:**
- Do NOT add `additionalProperties: false` to any object.
- Do NOT remove the existing `bestemt`/`ubestemt`/`definite`/`indefinite` flat string fields from `caseEntry.forms`.
- Do NOT make any new field `required`.
  </action>
  <verify>
1. Run `node -e "const s = JSON.parse(require('fs').readFileSync('vocabulary/schema/noun.schema.json','utf8')); const ce = s.$defs.caseEntry.properties; console.log('forms.singular:', !!ce.forms.properties.singular, 'forms.plural:', !!ce.forms.properties.plural); console.log('caseEntry.bestemt:', !!ce.bestemt, 'caseEntry.ubestemt:', !!ce.ubestemt); console.log('weak_masculine:', !!s.$defs.nounEntry.properties.weak_masculine);"` — all fields exist.
2. Run `node -e "const Ajv = require('ajv/dist/2020.js').default; const ajv = new Ajv({strict:false,allErrors:true}); const core = JSON.parse(require('fs').readFileSync('vocabulary/schema/core-word.schema.json','utf8')); const schema = JSON.parse(require('fs').readFileSync('vocabulary/schema/noun.schema.json','utf8')); const data = JSON.parse(require('fs').readFileSync('vocabulary/core/de/nounbank.json','utf8')); ajv.addSchema(core,'core-word.schema.json'); const v = ajv.compile(schema); console.log('valid:', v(data)); if(!v(data)) console.log(v.errors);"` — existing nounbank data still validates.
  </verify>
  <done>noun.schema.json has singular/plural nullable sub-objects inside caseEntry.forms (alongside legacy flat fields), bestemt/ubestemt documented directly on caseEntry, and weak_masculine boolean on nounEntry. All 331 existing noun entries still pass schema validation.</done>
</task>

<task type="auto">
  <name>Task 3: Register grammar_noun_declension and grammar_genitiv features</name>
  <files>vocabulary/grammar-features.json</files>
  <action>
Edit `vocabulary/grammar-features.json` to add two new feature entries to the `de.features` array.

Insert the two entries **after** the existing `grammar_dative` entry (which is currently the last noun-category feature, at array index 9) and **before** the `grammar_comparative` entry (array index 10). This groups all noun features together.

**Entry 1 — grammar_noun_declension (parent feature, insert first):**

```json
{
  "id": "grammar_noun_declension",
  "name": "Kasusbøyning",
  "nameEn": "Noun Declension",
  "description": "Kasusbøyning av substantiv (nominativ, akkusativ, dativ, genitiv)",
  "descriptionEn": "Noun case declension (nominative, accusative, dative, genitive)",
  "category": "nouns",
  "appliesTo": ["noun"],
  "dataPath": "cases"
}
```

**Entry 2 — grammar_genitiv (child feature with dependsOn, insert second):**

```json
{
  "id": "grammar_genitiv",
  "name": "Genitiv",
  "nameEn": "Genitive Case",
  "description": "Genitivbøyning av substantiv (des/der/des)",
  "descriptionEn": "Genitive case declension of nouns",
  "category": "nouns",
  "appliesTo": ["noun"],
  "dataPath": "cases.genitiv",
  "dependsOn": "grammar_noun_declension"
}
```

**Critical details:**
- Use Norwegian `ø` in "Kasusbøyning" and "Genitivbøyning" — consistent with existing "Adjektivbøyning" in the same file.
- `grammar_genitiv` MUST have `"dependsOn": "grammar_noun_declension"` — follows the exact pattern of `grammar_adjective_genitive` depending on `grammar_adjective_declension`.
- `grammar_noun_declension` MUST appear before `grammar_genitiv` in the array.
- Also update `_metadata.updatedAt` to `"2026-02-22"`.
  </action>
  <verify>
1. Run `node -e "const f = JSON.parse(require('fs').readFileSync('vocabulary/grammar-features.json','utf8')); const ids = f.de.features.map(x=>x.id); console.log('has noun_declension:', ids.includes('grammar_noun_declension')); console.log('has genitiv:', ids.includes('grammar_genitiv')); const gn = f.de.features.find(x=>x.id==='grammar_genitiv'); console.log('dependsOn:', gn.dependsOn); console.log('order ok:', ids.indexOf('grammar_noun_declension') < ids.indexOf('grammar_genitiv')); console.log('total de features:', f.de.features.length);"` — both features exist, dependsOn is correct, noun_declension precedes genitiv, total features = 16.
2. Verify JSON is valid: `node -e "JSON.parse(require('fs').readFileSync('vocabulary/grammar-features.json','utf8')); console.log('Valid JSON');"`.
  </verify>
  <done>grammar-features.json has grammar_noun_declension and grammar_genitiv entries under de.features, with correct Norwegian ø characters, proper dependsOn relationship, and positioned after existing noun features. Total German feature count is 16 (was 14).</done>
</task>

</tasks>

<verification>
1. **Verb schema non-breaking:** Run AJV validation of existing verbbank.json against updated verb.schema.json — zero errors.
2. **Noun schema non-breaking:** Run AJV validation of existing nounbank.json against updated noun.schema.json — zero errors.
3. **Grammar features valid:** grammar-features.json parses as valid JSON with 16 German features (14 existing + 2 new).
4. **No additionalProperties: false added:** Grep verb.schema.json and noun.schema.json for "additionalProperties.*false" — zero matches (existing schemas don't use this and we must not introduce it).
5. **Field count check:** verb.schema.json tenseConjugation has 8 properties (3 existing + 5 new). noun.schema.json caseEntry has 5 direct properties (intro, feature, forms, bestemt, ubestemt) and forms has 6 sub-properties (4 existing flat + 2 new nested).
</verification>

<success_criteria>
- verb.schema.json: tenseConjugation has auxiliary/participle/auxiliary_note/dual_auxiliary/modal_note; verbEntry has inseparable
- noun.schema.json: caseEntry.forms has singular/plural nullable sub-objects; caseEntry has bestemt/ubestemt directly; nounEntry has weak_masculine
- grammar-features.json: de.features includes grammar_noun_declension and grammar_genitiv with dependsOn relationship
- All existing data (148 verbs, 331 nouns) continues to pass AJV validation against updated schemas
</success_criteria>

<output>
After completion, create `.planning/phases/11-schema-extensions/11-01-SUMMARY.md`
</output>
