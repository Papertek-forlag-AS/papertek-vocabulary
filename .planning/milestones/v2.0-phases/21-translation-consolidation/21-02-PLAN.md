---
phase: 21-translation-consolidation
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified: []
autonomous: true
requirements: [TRANS-03]

must_haves:
  truths:
    - "No de-nb-dict/ directory exists under vocabulary/translations/"
    - "No de-en-dict/ directory exists under vocabulary/translations/"
    - "No vocabulary/dictionary/de/ directory exists (old bank location)"
    - "All existing functionality still works: search index builds, API handlers resolve translations"
  artifacts: []
  key_links: []
---

<objective>
Verify the merged translation data is correct, then delete the old -dict/ translation directories and the old vocabulary/dictionary/de/ directory.

Purpose: Complete the translation consolidation by removing duplicate directories, confirming no data loss.
Output: Old directories removed, verification report confirming data integrity.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-translation-consolidation/21-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify merged translations contain all dict entries</name>
  <files></files>
  <action>
    Run verification checks to confirm every entry from the -dict/ directories is now present in the merged core directories. This is a read-only verification step — no files are modified.

    **Verification script (inline node -e or standalone):**

    For each language pair (de-nb, de-en) and each dict bank (nounbank, verbbank, generalbank):
    1. Read the -dict/ file (e.g., `vocabulary/translations/de-nb-dict/nounbank.json`)
    2. Read the merged file (e.g., `vocabulary/translations/de-nb/nounbank.json`)
    3. For every entry key in the dict file (excluding `_metadata`), assert it exists in the merged file
    4. For every entry key in the dict file, assert `merged[key].translation === dict[key].translation` (data preserved exactly)
    5. Count: merged entries should equal core + dict entries (no loss, no duplicates)

    **Cross-check against banks:**
    Compare merged translation entry counts against the merged bank entry counts from `vocabulary/banks/de/`:
    - nounbank: 1641 bank entries, merged translations should cover all or most
    - verbbank: 679 bank entries
    - generalbank: 673 bank entries (de-nb), 674 (de-en)
    Log any bank entries missing translations (informational).

    **Search index rebuild test:**
    Run `npm run build:search-index` and verify:
    - 3454 total entries
    - 144 verbs with pp
    - Translation coverage: count entries with tr.nb and tr.en fields

    All checks must pass before proceeding to deletion.
  </action>
  <verify>
    1. Every dict entry key is found in the merged file (0 missing)
    2. Every dict entry's translation field matches exactly
    3. Search index builds successfully with 3454 entries
    4. Translation coverage report logged
  </verify>
  <done>
    All -dict/ entries confirmed present in merged directories. Search index builds correctly from merged data. Translation gap report generated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete old -dict/ directories and vocabulary/dictionary/de/</name>
  <files></files>
  <action>
    After Task 1 verification passes, delete the old directories:

    **1. Delete -dict/ translation directories:**
    ```
    rm -rf vocabulary/translations/de-nb-dict/
    rm -rf vocabulary/translations/de-en-dict/
    ```

    **2. Delete old vocabulary/dictionary/de/ directory:**
    This directory contains the old separate dictionary bank files (adjectivebank.json, nounbank.json, verbbank.json, etc.) and the search-index.json that have been superseded by `vocabulary/banks/de/` (from Phase 20).
    ```
    rm -rf vocabulary/dictionary/de/
    ```
    If vocabulary/dictionary/ is now empty, remove it too.

    **3. Post-deletion verification:**
    - Confirm `ls vocabulary/translations/de-nb-dict/ 2>/dev/null` returns nothing
    - Confirm `ls vocabulary/translations/de-en-dict/ 2>/dev/null` returns nothing
    - Confirm `ls vocabulary/dictionary/de/ 2>/dev/null` returns nothing
    - Run `npm run build:search-index` — must still produce 3454 entries (confirms no code depends on deleted dirs)
    - The v1 manifest handler (`api/vocab/v1/manifest.js`) reads `vocabulary/translations/` directory listing. Confirm it no longer sees -dict/ dirs (they're gone, so its `readdirSync` will only return de-nb, de-en, es-en, es-nb, fr-nb).

    **Note:** Do NOT delete `vocabulary/core/de/` — that's still needed by other code paths (v1 core handler reads from there). Its cleanup is Phase 23 scope (BANK-06).

    **Note:** The `vocabulary/dictionary/de/search-index.json` grep from `scripts/verify-integration.js` will break, but that script is a historical one-shot from Phase 15. It is not run in production.
  </action>
  <verify>
    1. `ls vocabulary/translations/de-nb-dict/ 2>/dev/null` returns empty (dir gone)
    2. `ls vocabulary/translations/de-en-dict/ 2>/dev/null` returns empty (dir gone)
    3. `ls vocabulary/dictionary/de/ 2>/dev/null` returns empty (dir gone)
    4. `npm run build:search-index` still outputs 3454 entries, 144 verbs with pp
    5. `find vocabulary/translations -name "*-dict*" -type d` returns nothing
  </verify>
  <done>
    Old de-nb-dict/, de-en-dict/, and vocabulary/dictionary/de/ directories are permanently removed. All active code continues to function correctly.
  </done>
</task>

</tasks>

<verification>
1. Zero data loss: every entry from -dict/ files verified present in merged files before deletion
2. Directories removed: no -dict/ translation dirs, no vocabulary/dictionary/de/
3. Active code works: search index builds, API handlers function without errors
4. Translation completeness: gap report available showing any bank entries without translations
</verification>

<success_criteria>
- All -dict/ translation directory entries verified present in merged files
- de-nb-dict/ and de-en-dict/ directories deleted
- vocabulary/dictionary/de/ directory deleted
- Search index rebuild confirms 3454 entries post-deletion
- No active code path broken by deletions
</success_criteria>

<output>
After completion, create `.planning/phases/21-translation-consolidation/21-02-SUMMARY.md`
</output>
