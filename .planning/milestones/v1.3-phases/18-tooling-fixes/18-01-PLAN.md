---
phase: 18-tooling-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json]
autonomous: true
requirements: [TOOL-01, TOOL-02]

must_haves:
  truths:
    - "Running `npm run build:search-index` executes scripts/build-search-index.js and exits 0"
    - "Running `npm run validate:all` runs noun, verb, adjective, dict variant, and integration validations — all pass"
    - "No npm scripts reference non-existent files (validate.js, check-ids.js, check-audio.js are gone)"
  artifacts:
    - path: "package.json"
      provides: "All npm script entries for project tooling"
      contains: "build:search-index"
  key_links:
    - from: "package.json build:search-index"
      to: "scripts/build-search-index.js"
      via: "npm run"
      pattern: "build:search-index.*build-search-index\\.js"
    - from: "package.json validate:all"
      to: "scripts/validate-nouns.js, scripts/validate-verbs.js, scripts/validate-adjectives.js, scripts/verify-integration.js"
      via: "npm run chaining"
      pattern: "validate:all"
---

<objective>
Register all project scripts in package.json and build a unified validate:all command.

Purpose: Make all tooling discoverable via `npm run` and provide a single command that runs the full validation suite — nouns, verbs, adjectives, dict variants, and integration checks.

Output: Updated package.json with correct, complete npm scripts section.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@scripts/build-search-index.js
@scripts/validate-nouns.js
@scripts/validate-verbs.js
@scripts/validate-adjectives.js
@scripts/verify-integration.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register build:search-index and clean up phantom scripts</name>
  <files>package.json</files>
  <action>
Edit the `scripts` section of package.json:

1. **Add** `build:search-index` script: `"build:search-index": "node scripts/build-search-index.js"`

2. **Remove** three phantom scripts that reference non-existent files:
   - `"validate": "node scripts/validate.js"` — file does not exist
   - `"validate:ids": "node scripts/check-ids.js"` — file does not exist
   - `"validate:audio": "node scripts/check-audio.js"` — file does not exist

3. **Add** missing adjective validation script: `"validate:adjectives": "node scripts/validate-adjectives.js"`

Keep all existing valid scripts unchanged:
- `dev`, `validate:nouns`, `validate:nouns:dict`, `validate:verbs`, `validate:verbs:dict`, `verify:integration`

Do NOT remove `validate:all` yet — Task 2 rewrites it.
  </action>
  <verify>
Run `npm run build:search-index` — should exit 0 and print "Total entries: 3454".
Run `npm run validate:adjectives` — should print "PASS: All 365 adjective entries validate against schema".
Confirm phantom scripts are gone: `npm run validate 2>&1` should show "Missing script: validate" error.
  </verify>
  <done>
`build:search-index` npm script exists and runs build-search-index.js successfully. Three phantom scripts removed. `validate:adjectives` script registered.
  </done>
</task>

<task type="auto">
  <name>Task 2: Unify validate:all to cover full validation suite</name>
  <files>package.json</files>
  <action>
Rewrite the `validate:all` script in package.json to chain ALL working validation scripts in sequence:

```
"validate:all": "npm run validate:nouns && npm run validate:verbs && npm run validate:adjectives && npm run validate:nouns:dict && npm run validate:verbs:dict && npm run verify:integration"
```

Order rationale: core banks first (nouns, verbs, adjectives), then dict variants (nouns:dict, verbs:dict), then integration last (it depends on all banks being valid).

Use `&&` chaining so the command fails fast on first error — if core nouns fail, no point running dict or integration.
  </action>
  <verify>
Run `npm run validate:all` — all 6 validation scripts should execute in sequence:
1. "PASS: All 331 noun entries validate against schema"
2. "PASS: All 148 verb entries validate against schema"
3. "PASS: All 365 adjective entries validate against schema"
4. "PASS: All ... noun entries validate" (dict)
5. "PASS: All ... verb entries validate" (dict)
6. Multiple "PASS: SYNC-0X: ..." lines from verify-integration.js

Final exit code should be 0.
  </verify>
  <done>
`npm run validate:all` executes nouns, verbs, adjectives, dict variants, and integration checks — all passing — in a single command.
  </done>
</task>

</tasks>

<verification>
1. `npm run build:search-index` exits 0 — TOOL-01 satisfied
2. `npm run validate:all` exits 0 and runs all 6 validators — TOOL-02 satisfied
3. `cat package.json | grep -c "validate.js\|check-ids.js\|check-audio.js"` returns 0 — no phantom scripts
4. `npm run` lists all scripts — verify no orphaned entries
</verification>

<success_criteria>
- `npm run build:search-index` executes without error
- `npm run validate:all` runs the full validation suite (nouns, verbs, adjectives, dict variants, integration) and all pass
- No npm scripts reference non-existent files
- package.json is valid JSON
</success_criteria>

<output>
After completion, create `.planning/phases/18-tooling-fixes/18-01-SUMMARY.md`
</output>
