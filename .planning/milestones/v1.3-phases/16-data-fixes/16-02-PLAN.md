---
phase: 16-data-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vocabulary/core/de/verbbank.json
  - vocabulary/dictionary/de/verbbank.json
  - vocabulary/schema/verb.schema.json
  - scripts/fix-verb-fields.js
autonomous: true
requirements:
  - DATA-02
  - DATA-03

must_haves:
  truths:
    - "All 148 core verbbank entries have a type field using the classification: regular, irregular, modal, separable, reflexive, or verbphrase"
    - "Verbs fitting multiple categories use priority order: reflexive > separable > modal > irregular > regular"
    - "Verbs with secondary traits have a tags array capturing traits not in the primary type"
    - "All 12 non-verbphrase verbs missing presens now have complete presens conjugation objects with all 6 pronoun forms"
    - "Dict verbbank entries mirror the type, tags, and presens changes from core"
  artifacts:
    - path: "scripts/fix-verb-fields.js"
      provides: "Audit-trail script for verb type classification and presens fixes"
    - path: "vocabulary/schema/verb.schema.json"
      provides: "Updated schema with tags array property"
      contains: "tags"
    - path: "vocabulary/core/de/verbbank.json"
      provides: "All 148 entries with type field and 12 entries with new presens"
    - path: "vocabulary/dictionary/de/verbbank.json"
      provides: "Dict entries synced with core type, tags, and presens"
  key_links:
    - from: "scripts/fix-verb-fields.js"
      to: "vocabulary/core/de/verbbank.json"
      via: "read-classify-write"
      pattern: "readFileSync.*verbbank"
    - from: "scripts/fix-verb-fields.js"
      to: "vocabulary/dictionary/de/verbbank.json"
      via: "sync type and presens to dict"
      pattern: "readFileSync.*dictionary.*verbbank"
---

<objective>
Add verb type classification to all 148 core verbbank entries and fill 12 missing presens conjugations. Reclassify existing strong/weak types to irregular/regular per user decision. Add tags array for secondary traits. Sync all changes to dict verbbank.

Purpose: DATA-02 requires every verb entry to have a type field. DATA-03 requires all 144 non-verbphrase verbs to have presens conjugations. Currently 72 entries lack a type field, existing types use strong/weak instead of irregular/regular, and 12 verbs are missing presens.
Output: Complete type classification + presens conjugations + schema update + audit-trail script.
</objective>

<execution_context>
@/Users/geirforbord/.claude/get-shit-done/workflows/execute-plan.md
@/Users/geirforbord/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-data-fixes/16-CONTEXT.md
@vocabulary/core/de/verbbank.json
@vocabulary/dictionary/de/verbbank.json
@vocabulary/schema/verb.schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update verb schema to support tags array</name>
  <files>vocabulary/schema/verb.schema.json</files>
  <action>
Add a `tags` property to the verbEntry definition in `vocabulary/schema/verb.schema.json`.

In the `verbEntry.properties` object, after the `type` property, add:
```json
"tags": {
  "type": "array",
  "description": "Secondary verb traits not captured by primary type (e.g., type: separable, tags: [irregular])",
  "items": {
    "type": "string",
    "enum": ["regular", "irregular", "modal", "separable", "reflexive", "inseparable"]
  }
}
```

Do NOT change the existing `type` enum — it already includes all needed values: `["strong", "weak", "modal", "reflexive", "separable", "auxiliary", "regular", "irregular", "verbphrase"]`. The strong/weak values remain valid in the enum for backward compatibility even though this phase reclassifies them to irregular/regular.
  </action>
  <verify>
Run: `npm run validate:verbs` — should pass with no new errors (tags is optional, so existing entries without it remain valid).
  </verify>
  <done>
verb.schema.json has a tags array property. AJV validation continues to pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create verb fix script — classify types, add presens, sync to dict</name>
  <files>
    scripts/fix-verb-fields.js
    vocabulary/core/de/verbbank.json
    vocabulary/dictionary/de/verbbank.json
  </files>
  <action>
Create `scripts/fix-verb-fields.js` (ESM) as an audit-trail script that:

**Part A — Type Classification (DATA-02)**

Classify all 148 core verbbank entries using the priority order from the user decision: reflexive > separable > modal > irregular > regular.

Classification rules:
1. **verbphrase**: 4 entries with `_verbphrase` key suffix (`rad_fahren_verbphrase`, `musik_hoeren_verbphrase`, `gassi_gehen_verbphrase`, `fertig_werden_verbphrase`) — keep as `type: "verbphrase"`
2. **modal**: 7 entries already typed as modal (`moegen_verb`, `koennen_verb`, `muessen_verb`, `wollen_verb`, `duerfen_verb`, `sollen_verb`, `moechten_modal`) — keep as `type: "modal"`
3. **reflexive**: 26 entries with `sich_` key prefix — set `type: "reflexive"`. If also separable (e.g., `sich_anziehen_verb`, `sich_aufregen_verb`, `sich_ausruhen_verb`), add `tags: ["separable"]`. If also irregular (strong verb stem change), add `"irregular"` to tags.
4. **separable**: Non-reflexive verbs with separable prefixes (an-, auf-, aus-, ein-, mit-, weg-, fern-, um-, zu-). Identified entries: `anfangen_verb`, `anrufen_verb`, `aufwachen_verb`, `aufstehen_verb`, `aufraeumen_verb`, `ausziehen_verb`, `einladen_verb`, `einpacken_verb`, `einkaufen_verb`, `einschlafen_verb`, `fernsehen_verb`, `mitnehmen_verb`, `mitbringen_verb`, `mitkommen_verb`, `wegfahren_verb`. Set `type: "separable"`. If also irregular (strong stem), add `tags: ["irregular"]`.
5. **irregular** (formerly "strong"): Verbs with stem vowel changes in conjugation. The 32 entries currently typed as `strong` should become `type: "irregular"`. Also classify currently untyped strong verbs: `finden_verb`, `beginnen_verb`, `schwimmen_verb`, `singen_verb`, `trinken_verb`, `fliegen_verb`, `scheinen_verb`, `bleiben_verb`, `bringen_verb`. Plus mixed verbs: `kennen_verb` (kannte), `wissen_verb` (already typed strong → irregular).
6. **regular** (formerly "weak"): All remaining verbs with regular conjugation patterns. The 31 entries currently typed as `weak` should become `type: "regular"` (after filtering out any that should be reclassified as separable/reflexive first). All currently untyped regular verbs: `spielen_verb`, `klingeln_verb`, `tanzen_verb`, `kochen_verb`, `malen_verb`, `machen_verb`, `kaufen_verb`, `brauchen_verb`, `hoeren_verb`, `hoffen_verb`, `fuettern_verb`, `streicheln_verb`, `leben_verb`, `oeffnen_verb`, `jagen_verb`, `wandern_verb`, `traeumen_verb`, `sagen_verb`, `packen_verb`, `lieben_verb`, `stimmen_verb`, `dekorieren_verb`, `dauern_verb`, `danken_verb`, `antworten_verb`, `vertrauen_verb`, `erklaeren_verb`, `zeigen_verb`, `erzaehlen_verb`, `reisen_verb`, `klettern_verb`.

Important reclassifications from existing data:
- `bekommen_verb`: currently `type: "weak"` but it's actually irregular (bekam/bekommen) AND inseparable — set `type: "irregular"` (inseparable already flagged via `inseparable: true`)
- `wegfahren_verb`: currently `type: "weak"` but it's separable + irregular (fahren is strong) — set `type: "separable"`, `tags: ["irregular"]`
- `mitkommen_verb`: currently `type: "weak"` but it's separable + irregular (kommen is strong) — set `type: "separable"`, `tags: ["irregular"]`
- `einschlafen_verb`: currently `type: "weak"` but it's separable + irregular (schlafen is strong) — set `type: "separable"`, `tags: ["irregular"]`
- `ausziehen_verb`: currently `type: "weak"` but it's separable + irregular (ziehen is strong) — set `type: "separable"`, `tags: ["irregular"]`
- `liegen_bleiben_verb`: currently `type: "weak"` but it's a compound separable verb — set `type: "separable"`, `tags: ["irregular"]` (bleiben + liegen both strong)
- `spazieren_gehen_verb`: currently `type: "weak"` but it's a compound verb with gehen (strong) — set `type: "separable"`, `tags: ["irregular"]`
- `zaehneputzen_verb`: currently `type: "weak"` but it's separable (Zähne putzen) — set `type: "separable"` (putzen is regular, no irregular tag)
- `sich_aergern_ueber_verb`: currently `type: "weak"` — reflexive takes priority, set `type: "reflexive"`
- `interessieren_verb`: currently `type: "reflexive"` — keep (it's the non-reflexive form but bank classifies it as reflexive)
- `entspannen_verb`: currently `type: "reflexive"` — keep

For the `tags` array: only add when a verb has a secondary trait. If verb is simply `type: "regular"` with no secondary traits, do NOT add an empty tags array.

Insert the `type` field early in the entry object (after `word` or after `audio`, consistent with existing entries that have type). Insert `tags` after `type` when present.

**Part B — Presens Conjugations (DATA-03)**

Add presens conjugation objects for these 12 non-verbphrase verbs that currently lack presens. Use the established `former` + `feature` structure:

```json
"presens": {
  "former": {
    "ich": "...",
    "du": "...",
    "er/sie/es": "...",
    "wir": "...",
    "ihr": "...",
    "sie/Sie": "..."
  },
  "feature": "grammar_presens"
}
```

The 12 verbs and their presens conjugations (Claude to verify using German grammar knowledge):

1. `reisen_verb` (regular): ich reise, du reist, er/sie/es reist, wir reisen, ihr reist, sie/Sie reisen
2. `fernsehen_verb` (separable: sehe fern): ich sehe fern, du siehst fern, er/sie/es sieht fern, wir sehen fern, ihr seht fern, sie/Sie sehen fern
3. `bekommen_verb` (irregular): ich bekomme, du bekommst, er/sie/es bekommt, wir bekommen, ihr bekommt, sie/Sie bekommen
4. `klettern_verb` (regular): ich klettere, du kletterst, er/sie/es klettert, wir klettern, ihr klettert, sie/Sie klettern
5. `wegfahren_verb` (separable + irregular, a→ä): ich fahre weg, du fährst weg, er/sie/es fährt weg, wir fahren weg, ihr fahrt weg, sie/Sie fahren weg
6. `liegen_bleiben_verb` (separable compound): ich bleibe liegen, du bleibst liegen, er/sie/es bleibt liegen, wir bleiben liegen, ihr bleibt liegen, sie/Sie bleiben liegen
7. `sich_aergern_ueber_verb` (reflexive): ich ärgere mich über, du ärgerst dich über, er/sie/es ärgert sich über, wir ärgern uns über, ihr ärgert euch über, sie/Sie ärgern sich über
8. `mitkommen_verb` (separable): ich komme mit, du kommst mit, er/sie/es kommt mit, wir kommen mit, ihr kommt mit, sie/Sie kommen mit
9. `spazieren_gehen_verb` (separable compound): ich gehe spazieren, du gehst spazieren, er/sie/es geht spazieren, wir gehen spazieren, ihr geht spazieren, sie/Sie gehen spazieren
10. `zaehneputzen_verb` (separable): ich putze Zähne, du putzt Zähne, er/sie/es putzt Zähne, wir putzen Zähne, ihr putzt Zähne, sie/Sie putzen Zähne — NOTE: Check existing verb bank for Zähneputzen convention. Some treat it as a single word "ich putze mir die Zähne" or simply "ich putze Zähne". Look at preteritum/perfektum forms already in the entry to follow the established pattern.
11. `ausziehen_verb` (separable + irregular): ich ziehe aus, du ziehst aus, er/sie/es zieht aus, wir ziehen aus, ihr zieht aus, sie/Sie ziehen aus
12. `einschlafen_verb` (separable + irregular, a→ä): ich schlafe ein, du schläfst ein, er/sie/es schläft ein, wir schlafen ein, ihr schlaft ein, sie/Sie schlafen ein

Place the `presens` object as the FIRST key under `conjugations` (before preteritum and perfektum), consistent with tense chronological order and the pattern used by verbs that already have presens.

Per user decision: "When touching the 12 verbs to add presens conjugations, also verify type, other conjugations, and all required fields on those entries."

**Part C — Sync to Dict Verbbank**

After applying fixes to core verbbank:
1. Read dict verbbank
2. For each core entry that was modified (type changed, tags added, presens added):
   - If the entry exists in dict verbbank, sync the `type`, `tags` (if present), and `presens` fields
   - Follow the sync-preteritum.js pattern: skip `_metadata`, only update entries that exist in both banks
3. Write dict verbbank

**Script pattern**: Follow established audit-trail pattern (ESM, read-modify-write, console.log summary with counts: "X entries reclassified, Y entries newly typed, Z presens conjugations added, W dict entries synced").

Run the script after creation: `node scripts/fix-verb-fields.js`
  </action>
  <verify>
Run: `node scripts/fix-verb-fields.js`
Then verify:
- `npm run validate:verbs` passes (core verbbank)
- `npm run validate:verbs:dict` passes (dict verbbank)
- Spot check: `node -e 'import("fs").then(fs => { const d = JSON.parse(fs.readFileSync("vocabulary/core/de/verbbank.json","utf8")); const types = {}; Object.keys(d).filter(k=>k!="_metadata").forEach(k => { types[d[k].type] = (types[d[k].type]||0)+1 }); console.log(types) })'` — should show no undefined types, all 148 classified
- Spot check: All 12 previously-missing presens entries now have `conjugations.presens.former` with 6 keys
  </verify>
  <done>
All 148 core verbbank entries have a type field (regular/irregular/modal/separable/reflexive/verbphrase). Secondary traits captured in tags arrays. 12 verbs now have complete presens conjugations. Dict verbbank synced. AJV validation passes for both banks. Zero entries with missing type. Zero non-verbphrase entries missing presens.
  </done>
</task>

</tasks>

<verification>
1. `node scripts/fix-verb-fields.js` runs without error
2. `npm run validate:verbs` passes (core)
3. `npm run validate:verbs:dict` passes (dict)
4. All 148 entries have a `type` field — zero undefined
5. Type distribution matches expected: ~26 reflexive, ~15 separable, 7 modal, 4 verbphrase, remaining split between irregular and regular
6. No entry has both `type: "strong"` or `type: "weak"` (fully reclassified)
7. All 12 previously-missing presens entries now have complete presens.former with 6 pronoun keys
8. Tags arrays present only on multi-trait verbs (not on single-trait entries)
</verification>

<success_criteria>
DATA-02 satisfied: all 148 entries typed. DATA-03 satisfied: all 144 non-verbphrase verbs have presens with 6 pronoun forms. Verb schema supports tags. Dict bank synced. AJV validation clean.
</success_criteria>

<output>
After completion, create `.planning/phases/16-data-fixes/16-02-SUMMARY.md`
</output>
